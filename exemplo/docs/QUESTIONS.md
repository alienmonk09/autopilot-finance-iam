# Dúvidas de spec e suposições adotadas

Formato: `- [N.M] dúvida → suposição adotada (data)`. O humano revisa e, se discordar, cria task corretiva no ROADMAP.

- [9.F1] Sem Chrome garantido no ambiente autônomo para rodar Lighthouse real com scores ≥ 90 → suposição: aceite demonstrado por proxy automatizado (budgets de ms/queries/CDN/paginação/assets em `tests/Feature/Performance/LighthouseBudgetTest.php`) + relatórios em `docs/lighthouse/` com comando de reprodução (`npx lighthouse <url> --form-factor=mobile`); scores reais a confirmar manualmente (2026-09-04).

- [0.3] SPEC 1 diz "locale pt_BR" sem definir fallback → suposição: `fallback_locale` também `pt_BR` (coerência total) (2026-09-03).
- [0.3] Task pede `.env.example` com chave `TIMEZONE`, mas o Laravel usa `APP_TIMEZONE` (o skeleton nem lia env para timezone) → suposição: `config/app.php` lê `env('APP_TIMEZONE', 'America/Sao_Paulo')` e `.env.example` expõe `APP_TIMEZONE` (2026-09-03).
- [0.3] SPEC manda "Fortify (ou auth do Filament)"; auth do Filament 5 exige Panel (`/admin`), que é a task 0.6 (fora de escopo) → suposição: Fortify headless + telas Blade próprias (2026-09-03).
- [0.3] 2FA: manter `confirmPassword => true` (padrão do scaffold Fortify) exige confirmação de senha recente para ativar/desativar → suposição: comportamento mantido por segurança; tela de confirmação implementada (2026-09-03).
- [0.4] SPEC 3.15 lista formatos de input mas não cobre casos-limite ("1.234" sem decimais, ".56", "12,34,56") nem forma de `toArray`/`jsonSerialize` → suposição: separador único com 3 dígitos à direita é milhar ("1.234" = 123400); fração aceita só 1–2 dígitos; resto inválido lança InvalidArgumentException; `jsonSerialize` retorna int de centavos e `toArray` retorna `['cents' => int]` (2026-09-03).
- [0.4] SPEC 3.15 exige exibir "R$ 1.234,56" mas o ICU pt_BR emite NBSP após "R$" e negativos como "-R$ ..." → suposição: `Money::format()` usa NumberFormatter e normaliza NBSP para espaço simples, garantindo a string exata da spec (2026-09-03).
- [0.5] Task diz "sem usuário = sem filtro extra"; alternativa seria fail-closed (negar tudo sem auth) → suposição: scope sem usuário autenticado não aplica filtro (fail-open em console/migrations/seeds/fila), e a proteção entre usuários fica no scope + policy com auth presente; `owns()` na policy é fail-closed (user_id ausente/divergente nega) (2026-09-03).
- [0.6] SPEC manda "Fortify (ou auth do Filament)" e a task pede login do painel com os mesmos usuários → suposição: painel usa login nativo do Filament (`->login()`, guard `web`, mesmo model `User` + `canAccessPanel` liberando o painel `app`); rotas Blade do Fortify (0.3) mantidas para cadastro/recuperação de senha fora do painel; sem `->registration()`/`->passwordReset()` no painel nesta task (2026-09-03).
- [0.6] SPEC 2.16 tem `theme` (light/dark/system) em `user_settings`, mas o Filament guarda o tema em localStorage com toggle próprio → suposição: nesta task vale o toggle padrão do Filament (default = tema do sistema); sincronizar com `user_settings` fica para task futura (exige JS/middleware dedicado) (2026-09-03).
- [0.6] SPEC 1 exige "sem CDN em produção" e a task pede fontes via Vite/CSS → suposição: sem `->font()` custom (o default usa Bunny CDN); painel usa a Inter Variable autohospedada no dist do Filament + `public/css/filament/app/theme.css` vanilla commitado via `->theme()` (zero build, zero CDN); migração para `->viteTheme()` (Tailwind) só quando houver build garantido no Docker/CI (2026-09-03).
- [1.1] SPEC 2.7 exige dedupe de payee por nome normalizado (lower, sem acento, trim) mas não diz se é unique no banco → suposição: validação em aplicação no `saving` (PayeeObserver lança PayeeException), sem unique no banco, pois o SQLite compara bytes e 'Pão' ≠ 'pao' para ele; unicidade vale por usuário (2026-09-03).
- [1.1] SPEC não define o destino de bancos privados e categorias do usuário ao excluir o usuário → suposição: `cascadeOnDelete` em banks.user_id e categories.user_id (LGPD 4.12: excluir a conta apaga tudo; banco privado virando global via nullOnDelete poluiria o catálogo) (2026-09-03).
- [1.1] SPEC 2.5 diz "máx. 2 níveis" sem detalhar as direções → suposição: CategoryDepthGuard rejeita neto (pai que já é subcategoria), auto-pai e dar pai a categoria que já tem filhos (2026-09-03).
- [1.2] SPEC 2.5 lista "Outros" na despesa e "Outros" na receita sem dizer se são uma ou duas linhas → suposição: UMA categoria "Outros" com type=both cobre os dois lados (2026-09-03).
- [1.2] SPEC 2.5 diz "Investimentos (aportes)" sem detalhar → suposição: topo "Investimentos" (expense) com sub "Aportes" (2026-09-03).
- [1.2] SPEC 2.1 pede code COMPE/ISPB "quando conhecido" → suposição: code null para instituições sem COMPE próprio conhecido (Neon, Next, Iti, corretoras digitais, genéricos) em vez de chutar código (2026-09-03).
- [1.3] SPEC 2.8/3.1 não define o sinal do `adjustment` (amount_cents é sempre positivo) → suposição: adjustment SOMA ao saldo; correção para baixo lança-se como `expense` (reconciliação com diferença negativa gera despesa, não ajuste negativo) (2026-09-03).
- [1.3] SPEC 3.1 não diz se `initial_balance_date` preenchida zera o saldo antes dela → suposição: se D < `initial_balance_date`, saldo é 0 (conta ainda não existia, lançamentos ignorados); se null, o inicial sempre conta (2026-09-03).
- [1.3] SPEC 3.1 não diz quem filtra lançamentos por conta/usuário no service → suposição: o service casa pelo próprio DTO (`account_id`/`transfer_account_id`; linhas de outras contas são ignoradas, sem filtro por `user_id` dentro do service — o chamador escopa por usuário) (2026-09-03).
- [1.3] SPEC 3.1 manda o previsto incluir "ocorrências ainda não materializadas + faturas em aberto" sem formato → suposição: `forecast()` recebe-as já convertidas no mesmo DTO `BalanceEntry` (`$projected`), e conta todos os status com `date <= D` (2026-09-03).
- [1.4] SPEC 4.12 pede merge de payees "movendo referências", mas a tabela `transactions` só existe na fase 2 → suposição: MergePayees nesta task valida mesmo usuário, preserva os dados do destino (adota categoria/notas do duplicado só se vazios) e apaga o duplicado em DB::transaction; mover `transactions.payee_id` entra na task que criar o vínculo (fase 2) (2026-09-03).
- [1.4] Coluna "Saldo atual" do Resource de contas usa AccountBalanceService com lista vazia de lançamentos (sem tabela `transactions` ainda, equivale ao saldo inicial); a task de transações (fase 2) passa a alimentar os BalanceEntry reais sem mudar a coluna (2026-09-03).
- [1.4] SPEC 2.1 não define slug de banco criado pelo usuário → suposição: slug gerado de `name` + sufixo aleatório (mesmo padrão da factory), fora do form (2026-09-03).
- [1.5] SPEC 3.13 manda "realocar para outra categoria" sem detalhar o destino → suposição: destino precisa ser visível ao mesmo usuário (categoria própria ou padrão do sistema); origem de sistema não pode ser realocada; se a origem tem subcategorias, o destino precisa ser de topo (sem neto, regra 2.5); `ReallocateCategory` só move vínculos, não apaga (a exclusão passa depois) (2026-09-03).
- [1.5] SPEC 3.13 não lista quais vínculos bloqueiam a exclusão de conta/cartão → suposição: bloqueia se QUALQUER tabela existente referenciar a conta (`transactions.account_id`/`transfer_account_id`, `credit_cards.payment_account_id`, `recurrences`, `installment_groups`, `goals`, `import_batches` — só as que existirem via `Schema::hasTable`, forward-compatible com a fase 1 sem `transactions`) (2026-09-03).
- [2.2] SPEC 2.8/2.9 e 3.9/3.10 não proíbem explicitamente categoria/splits em `transfer` → suposição: transfer nunca tem categoria nem splits (Create/Update rejeitam); category_id fica nulo (2026-09-03).
- [2.1] SPEC 2.8 pede `amount_cents` positivo sem dizer se é CHECK no banco ou validação de domínio → suposição: sem CHECK no schema (bigInteger simples); positividade validada no domínio (task 2.2, SPEC 3.15); factories geram valores positivos (2026-09-03).
- [2.1] SPEC 2.8 diz `competence_date` default = `date` sem dizer onde → suposição: coluna nullable no banco, preenchida no `creating` do model (nunca default no DB nem em controller) (2026-09-03).
- [2.1] SPEC 2.9 não define o comportamento da FK de `transaction_splits.category_id` → suposição: `nullOnDelete` (consistente com `transactions.category_id`); a exclusão de categoria em uso continua bloqueada pelo CategoryDeletionGuard, a FK é só rede de segurança (2026-09-03).
- [2.1] SPEC 2.8 pede `external_id` com "unique(user_id, external_id) ou index" → suposição: os dois (unique composto para o dedupe de importação + índice simples em `external_id` da lista de índices) (2026-09-03).
- [2.3b] SPEC 4.2 não diz o que entra nos totais do rodapé além de receita/despesa/resultado → suposição: receitas = income; despesas = expense + credit_card_expense; transfer, adjustment e credit_card_payment não entram (3.9/3.5); `is_ignored_in_reports` NÃO é considerado (rodapé reflete o filtro, não um relatório) (2026-09-03).
- [2.3b] SPEC 4.2 lista presets sem definir "semana" nem conflito com a navegação mensal → suposição: semana começa segunda-feira (Carbon pt-BR); preset/custom ativo sobrepõe o mês; sem período explícito vale o mês navegado (2026-09-03).
- [2.3b] SPEC 4.2 não diz se o filtro de categoria pega splits → suposição: sim (category_id direto OU via splits); filtro de conta pega transferência nos dois lados (origem OU destino); texto busca em description + notes (2026-09-03).
- [2.3c] SPEC 4.2 lista as 5 ações em massa sem detalhar transferências, escopo cruzado nem reconciled → suposições: (a) categorizar ignora transferências em vez de falhar o lote (transfer nunca tem categoria, 3.9); (b) ids de outro usuário/inexistentes são ignorados (escopo por user_id), mas alvo (categoria/tag/conta) de outro usuário lança TransactionException e desfaz o lote; (c) só markCleared nunca toca reconciled (3.11) — categorizar/mover/excluir não bloqueiam reconciled (bloqueio vem na task 2.6); (d) attach de tag não gera entrada no activitylog (pivot taggables não é fillable; gap conhecido, sem log manual forjado) (2026-09-03).
- [2.4] SPEC 4.2 pede "sugestão de categoria" do payee sem detalhar empates nem splits → suposições: (a) só o lançamento mais recente conta (date desc, id desc; transferência recente sem categoria rende null, sem buscar mais atrás); (b) lançamento só com splits sugere o split de maior valor, mas o aprendizado grava o PRIMEIRO split; (c) default apontando para categoria invisível/inexistente cai para a última usada (2026-09-03).
- [2.6] SPEC 3.11 não detalha escopo do lote nem o ajuste → suposições: (a) categorizar/mover/excluir em lote IGNORAM reconciled (pulam sem contar, sem quebrar o lote); markCleared já ignorava não-pending (sem mudança); addTag em lote NÃO foi alterado (etiqueta não é valor/data); edição/exclusão individual de reconciled lança TransactionException; (b) desconciliar reverte reconciled com date <= D que tocam a conta (origem OU destino em transfer) para cleared; (c) ajuste de diferença positiva = type `adjustment`, negativa = type `expense`, ambos com a categoria de sistema "Ajuste de saldo" (criada via firstOrCreate se o seeder ainda não rodou), status reconciled, data D, descrição "Ajuste de reconciliação"; diferença zero = sem ajuste (o que torna o reconcile idempotente); (d) transferência é linha única global: reconciliar uma ponta marca a linha (visível nas duas contas); (e) "Ajustar saldo" da SPEC 4.3 está dobrado dentro do confirmar do modal "Reconciliar" (sem ação separada); "Desconciliar" é ação própria com data (2026-09-03).
- [2.7] Task pede "notificar sucesso e redirecionar ou oferecer 'ver lançamento'" sem dizer qual → suposição: SEM redirect automático (o modal é global, o usuário pode estar em qualquer página); notificação de sucesso com botão "Ver lançamento" (abre a edição do registro) (2026-09-03).
- [3.1] SPEC 2.4 não lista `user_id` em invoices, mas a task exige "global scope por usuário (Invoice via cartão ou user_id próprio — decida)" e a SPEC 2 diz que tabelas de dados do usuário têm `user_id` → suposição: invoices tem `user_id` próprio (herdado do dono do cartão na factory/allocator futuro) + FK `credit_card_id` cascade; scope e policy diretos sem join (2026-09-03).
- [3.1] SPEC 2.3 diz flag `due_in_next_month` com "default calculado" sem fórmula, e a task lista as duas flags com "default true" → suposição: ambas default true no schema; o cálculo real do vencimento é do InvoiceAllocator (3.2a) (2026-09-03).
- [3.1] SPEC 2.8/`credit_card_payment` não diz se categoria/splits são proibidos ou opcionais (só que não são obrigatórios) → suposição: opcionais (validados se informados); só `transfer_account_id` é proibido nos dois tipos de cartão e `account_id` é proibido em `credit_card_expense` (2026-09-03).
- [3.2a] SPEC 2.4/3.2 não define o que é `reference_month` → suposição: YYYY-MM da `closing_date` (mês do fechamento), ex.: fatura que fecha 10/09 tem reference 2026-09 (2026-09-03).
- [3.2a] SPEC 3.2/3.3 não diz se a comparação usa o dia configurado ou o efetivo (clamp) em mês curto → suposição: a decisão fica/sai do mês usa o C efetivo `min(closing_day, dias do mês da compra)` (compra no último dia de fev com C=31 conta como "dia do fechamento"); o ramo do vencimento (`due_day <= closing_day`) usa os dias configurados, não o efetivo (2026-09-03).
- [3.2b] SPEC 2.4 diz que total_cents é "soma das transações vinculadas" sem dizer se pagamentos entram → suposição: `recalculateTotals` soma SÓ `credit_card_expense` e ignora `credit_card_payment` (pagamento é tracked via `paid_cents` na task 3.2c; somar pagamento no total inflaria a fatura) (2026-09-03).
- [3.2b] Task manda resolver para "a PRÓXIMA fatura" quando a alocada está fechada, sem dizer o que fazer se a próxima também estiver fechada → suposição: avança em loop (closing_date + 1 dia realocado via allocator) até achar/criar fatura `open`, com trava de 24 iterações (CardException se estourar) (2026-09-03).
- [3.2c] SPEC 3.4 manda descontar "parcelas futuras ainda não faturadas", mas a tabela `installment_groups` só existe na fase 4 e `transactions.installment_group_id` ainda não tem conceito de "não faturada" → suposição: `CreditCardLimitService::futureInstallments()` retorna 0; a contagem real entra na task de parcelamentos (fase 4); o campo já é exposto separadamente (`futureInstallmentsCents`) para a UI (2026-09-03).
- [3.2c] SPEC 3.5 cita categoria de sistema "Saldo anterior da fatura", mas o catálogo 2.5 só tem "Transferência", "Pagamento de fatura" e "Ajuste de saldo" → suposição: `PayInvoice` cria "Saldo anterior da fatura" via firstOrCreate (user_id null, sem pai, type both, is_system true, mesmo padrão do seeder); "Pagamento de fatura" reutiliza a do seed sem duplicar (2026-09-03).
- [3.2c] SPEC 3.5 diz que excedente "gera crédito (linha negativa na próxima fatura)", mas `amount_cents` é sempre positivo (validação de domínio, sem linha negativa possível) → suposição: excedente vira `paid_cents` na próxima fatura (usado = total − pago fica negativo e aumenta o disponível); sem transação sintética (2026-09-03).
- [3.2c] SPEC 3.5 não exige fatura fechada para pagar → suposição: `PayInvoice` aceita qualquer status exceto `paid` (pagar fatura `open` = antecipação, comportamento Nubank); só `paid` lança CardException (2026-09-03).
- [3.3] SPEC 3.6 diz "closed com due_date < hoje e não paga vira overdue" sem listar quais status contam como "não paga" nem o que acontece com `open` duplamente vencida → suposição: `paid` e `partially_paid` nunca viram `overdue`; `overdue` permanece `overdue`; igualdade com hoje não fecha nem vence; `open` com closing e due passados vai de `open` a `overdue` numa passada só (contada só como overdue no resumo) (2026-09-03).
- [3.5] Task manda vincular a compra "via InvoiceService::resolveForPurchase/attachTransaction" sem dizer onde (páginas ou domínio) → suposição: DENTRO de `CreateTransaction`/`UpdateTransaction` (resolve sem force + recálculo de totais nova/antiga), cobrindo form, modal rápido e futuros API/imports de uma vez; `CardException` interno vira `TransactionException` na fronteira; sem override manual "forçar nesta fatura" (gap registrado no report) (2026-09-03).
- [4.1] SPEC 2.11 não define on-delete das FKs de installment_groups → suposição: `user_id` cascadeOnDelete (LGPD 4.12, mesmo padrão de banks/categories); `credit_card_id`/`account_id`/`category_id`/`payee_id` nullOnDelete e `transactions.installment_group_id` nullOnDelete (rede de segurança; o bloqueio de exclusão em uso é do guard de domínio) (2026-09-03).
- [4.2a] SPEC 2.11 não define mínimo de parcelas ("gera N transações") → suposição: `installments_count >= 2` (1x é lançamento avulso, não parcelamento; count < 2 lança InstallmentException) (2026-09-03).
- [4.2a] SPEC não define o status das parcelas geradas → suposição: todas nascem `pending` (a regra 3.7 fala em excluir parcelas `pending`; efetivação é manual) (2026-09-03).
- [4.2a] SPEC 2.11 lista `category_id` sem dizer se é obrigatório (migration permite null) → suposição: obrigatório (parcelas seguem CreateTransaction: expense/credit_card_expense exigem categoria); payee opcional (2026-09-03).
- [4.2a] SPEC não padroniza a descrição de cada parcela → suposição: "{descrição do grupo} i/N" (ex.: "Notebook Dell 1/12") (2026-09-03).
- [4.2b] SPEC 2.11 manda "mover para a fatura atual" sem dizer se a data muda → suposição: no cartão muda SÓ o invoice_id (data original mantida); em conta muda a data para hoje (único campo móvel) (2026-09-03).
- [4.2b] "Fatura aberta atual" sem definição → suposição: a fatura `open` de menor `reference_month` do cartão; sem nenhuma, `InvoiceService::resolveForPurchase` para hoje (2026-09-03).
- [4.2b] Desconto/descrição/valores sem invariantes de borda → suposição: desconto que zeraria/negativaria qualquer antecipada lança InstallmentException (amount sempre positivo); `new_total_remaining_cents` exige > 0 e >= nº de futuras pendentes (≥ 1 centavo cada); parcela do cartão sem fatura (anômala) conta como deletável no cancel default (2026-09-03).
- [4.3] Task manda toggle "Parcelar" com "conta das abas Despesa/Receita", mas `CreateInstallmentPlan` (4.2a) só gera parcelas de despesa (`expense`/`credit_card_expense`) → suposição: toggle visível SÓ em Despesa e Cartão; parcelar receita exigiria estender o domínio (fora de escopo desta task); Transferência/Ajuste nunca parcelam (2026-09-03).
- [4.4] SPEC 3.4 manda descontar "parcelas futuras ainda não faturadas" sem definir o corte → suposição: contam SÓ as parcelas `credit_card_expense` pendentes (sem soft-delete) cuja fatura ainda é `open` (lookup de ids open do cartão + filtro user_id do dono); fatura `closed`/`partially_paid`/`overdue`/`paid` NÃO conta (o valor já está no `used` via `total_cents − paid_cents`); parcela efetivada, excluída ou sem fatura NÃO conta; parcela avulsa (sem grupo) em fatura open CONTA (é comprometido do mesmo jeito) (2026-09-03).
- [5.1] SPEC 2.10/3.8 diz "unique recurrence_id+recurrence_occurrence_date" mas o índice existente em transactions (task 2.1) é não-unique e a task 5.1 manda não recriar colunas/índices → suposição: o unique é lógico, garantido pelo job idempotente via verificação de existência antes de inserir (task 5.2); sem constraint unique no banco nesta task (evita rebuild com soft-deletes/nullable no SQLite) (2026-09-03).
- [5.1] SPEC 2.10 não define default de `auto_confirm` → suposição: `false` no schema (ocorrência nasce `pending` para confirmação, consistente com o default `pending` de transactions.status; regra fixa com valor certo usa `true`) (2026-09-03).
- [5.2] SPEC 2.10 não detalha `weekday` (valores, default, relação com start) → suposição: 0–6 padrão Carbon (0 = domingo); null herda o weekday do start (espaçamento exato de 7/14 dias); informado vale da 2ª ocorrência em diante, com a 1ª sempre = start_date (2026-09-03).
- [5.2] SPEC 2.10 não detalha `unit`/`interval` do custom nem default de `day_of_month` → suposição: unit aceita só `day`/`week`/`month` com `interval >= 1` (outro valor lança RecurrenceException); `day_of_month` null herda o dia do start; se `day_of_month` < dia do start, a 1ª ocorrência continua = start_date e o padrão vale das seguintes em diante (2026-09-03).
- [5.2] SPEC 3.8 não define o horizonte ("até hoje + generate_ahead_months") nem os campos de progresso → suposição: horizonte inclusivo `date <= hoje + generate_ahead_months` (addMonthsNoOverflow); `last_generated_date` = maior data programada até o horizonte; `next_occurrence_date` = primeira data estritamente após `last_generated_date` (null se a regra encerrou); regra com start futuro além do horizonte mantém `last` e aponta `next` = start (2026-09-03).
- [5.2] SPEC 3.8 não diz o que acontece com ocorrência deletada nem com falha numa regra → suposição: soft-delete não bloqueia (a próxima passada recria a ocorrência); falha numa regra (ex.: cartão sem fatura open) desfaz só a transação daquela regra e aborta a passada — o job pode repetir sem duplicar (2026-09-03).
- [5.3] SPEC 2.10 §109 não detalha o que cada escopo pode editar → suposição: "esta" edita 1 ocorrência (pending ou cleared; reconciled lança, SPEC 3.11) e detacha; "futuras" edita pendentes com occurrence_date >= alvo e detacha as tocadas (cleared/reconciled puladas em silêncio); "todas" atualiza o template + pendentes não-detached sem detachar (valores iguais ao template, job não as alteraria) e recria deletadas via gerador; nenhum escopo muda `type`/`start_date` (2026-09-03).
- [5.3] "Futuras" atualiza o template? → suposição: NÃO (só "todas" toca a Recurrence); novas ocorrências além do horizonte nascem com valores antigos até uma edição "todas" (2026-09-03).
- [5.3] "Todas" com novo fim (`end_date`/`max_occurrences` encurtado) remove pendentes além do fim? → suposição: NÃO remove (só atualiza valores das pendentes existentes); encerrar de fato é pausar a regra (2026-09-03).
- [5.3] Troca de canal (conta/cartão) pode migrar o tipo? → suposição: NÃO, só o canal compatível com o `type` (despesa/receita/ajuste = account_id; transfer = account_id + transfer_account_id distintos; cartão = credit_card_id com realocação de fatura); pagamento de fatura (`credit_card_payment`) aceita trocar a conta mas nunca o cartão nem a fatura (vínculo preservado) (2026-09-03).
- [5.3] Pausa remove detached pendentes futuras? Como (soft/hard)? → suposição: SIM, remove TODAS as pending futuras (literal da 3.8: "remove apenas pending futuras", detached incluso) via soft-delete (auditoria preservada; o job recria na retomada) (2026-09-03).
- [5.4] Adiar muda `date` ou `recurrence_occurrence_date`? → suposição: SÓ `date` (e a competência quando acompanhava a data); `recurrence_occurrence_date` é a identidade da ocorrência no job idempotente e nunca muda, senão o job recriaria a data original (2026-09-03).
- [5.4] Confirmar com valor ajustado marca `is_detached`? → suposição: SIM (ajuste manual, §109: ocorrência editada manualmente não é sobrescrita) (2026-09-03).
- [5.4] "Encerrar" não existe no domínio (só pausar/retomar) → suposição: encerrar = pausar (remove pendentes futuras, preserva passadas/efetivadas) + travar o fim em hoje via cascata "todas" (`end_type=on_date`, `end_date=hoje`), compondo os services existentes para a retomada não gerar nada novo (2026-09-03).
- [5.4] Competência acompanha o adiamento? → suposição: SIM, mas só quando era igual à data antiga (o default do model); competência alterada manualmente é preservada (2026-09-03).
- [5.4] Form de transação não tem campo "nome da regra" na SPEC → suposição: campo opcional `recurrence_name`; vazio cai para a descrição do lançamento (2026-09-03).
- [5.4] Toggle "Repetir" vale para quais tipos? E com "Parcelar" junto? → suposição: vale para TODOS os tipos (despesa fixa, salário, mesada); seções mutuamente exclusivas na UI e, em payload artesanal com os dois ligados, o parcelamento vence (comportamento anterior preservado) (2026-09-03).
- [5.4] Calendário e pendentes mostram ocorrências não materializadas? → suposição: NÃO, só materializadas (transactions com recurrence_id); a projeção é da task 5.5 (2026-09-03).
- [5.5] SPEC 3.1 manda o previsto incluir "ocorrências ainda não materializadas" sem detalhar status, detached/auto_confirm nem cartão → suposições: (a) projetadas entram com status Pending (previsto conta pending, coerente com [1.3]); (b) `auto_confirm` e `is_detached` só valem na materialização e são ignorados na projeção; (c) template `credit_card_expense` é pulado na projeção (o efeito no saldo de conta seria zero de qualquer forma via AccountBalanceService); (d) faturas em aberto no previsto ficam para task futura (fora do escopo desta task) (2026-09-03).
- [6.1] SPEC 2.12 não detalha as colunas de `budget_periods` nem se tem `user_id` próprio → suposição: `reference_month` string YYYY-MM (mesmo padrão de `invoices.reference_month`) e SEM `user_id` próprio (dono via `budget`; sem scope/policy/log próprios, acesso e auditoria passam pelo Budget) (2026-09-03).
- [6.1] SPEC 2.13 não diz se `goal_contributions` tem `user_id` → suposição: SEM `user_id` próprio (dono via `goal`; sem scope/policy/log próprios, acesso e auditoria passam pela Goal) (2026-09-03).
- [6.2] SPEC 2.12 lista só `is_ignored_in_reports` como exclusão do spent e não cita status → suposição: `pending` conta (só soft-deleted exclui além do ignored); transação com splits é contada pelos splits mesmo se `category_id` estiver preenchido (relatórios usam os splits, 3.10) (2026-09-03).
- [6.2] SPEC 3.12 não define limites das faixas, orçamento zerado, mês sem histórico nem escopo de inativos → suposições: faixas inclusivas (>= threshold, >= 100); `budgeted_cents` <= 0 com gasto > 0 = 100% (dispara as duas); sem período anterior o carried é 0; `run()`/alertas processam só budgets `is_active` (2026-09-03).
- [6.2] SPEC 2.16 prevê a tabela `notifications` padrão do Laravel mas o schema ainda não tinha a migration → suposição: migration padrão via `make:notifications-table` (infra exigida pelo `$user->notify()` da task); dedupe "uma vez por faixa" via type+data JSON, sem tabela nova (2026-09-03).
- [6.3] SPEC 4.7 fala em "mês anterior/atual" mas a linha Budget é persistente (sem mês próprio) → suposição: fonte = orçamento mensal ativo mais recente por categoria vigente no mês anterior (`start_date` até o fim dele, sem `end_date` anterior ao alvo); destino = sem mensal ativo da mesma categoria com `start_date` no mês alvo; só `period=monthly` é copiado (2026-09-03).
- [6.3] "Arredondamento inteiro simples" da média sem fórmula → suposição: `intdiv` (trunca o resto, mesmo padrão do percentual de alertas da 6.2) (2026-09-03).
- [6.3] SPEC 4.7 não define "restante" com rollover nem a base da % → suposição: restante = orçado + rollover acumulado − gasto; % = gasto sobre (orçado + carried), com denominador <= 0 valendo 100% se há gasto (mesma convenção da 6.2); orçado = `budgeted_cents` materializado ou `amount_cents` (2026-09-03).
- [6.3] "Sugestão preenchida/aplicável, não salva sem confirmação" sem formato → suposição: a Page exibe o painel de sugestões e só persiste via "Aplicar" (por linha ou todas); criar orçamento usa `start_date` = 1º dia do mês exibido (editável), mantendo a cadeia da cópia (2026-09-03).
- [6.4] SPEC 2.13 manda "progresso %, aporte mensal necessário, projeção de conclusão" sem fórmulas → suposições: total aportado = `initial_cents` + soma dos aportes; progresso = `intdiv(saved*100, target)` sem teto (a barra da UI limita a 100%); restante = `max(0, target - saved)`; aporte mensal = 0 se já batida, null sem `target_date`, restante integral se prazo <= hoje, senão `ceil(restante / meses incluindo o mês atual)`; projeção = null sem aportes (ritmo zero), hoje se já batida, senão ritmo médio = aportes / meses desde o 1º aporte (mín. 1) e data = hoje + `ceil(restante / ritmo)` meses (tudo em centavos inteiros, sem float) (2026-09-03).
- [6.4] SPEC 2.13 diz que o aporte "pode criar automaticamente uma transferência" sem detalhar tipo/status/descrição → suposição: transferência single-row `cleared` origem → conta vinculada da meta, descrição "Aporte {nome da meta}", mesma data do aporte; meta sem conta vinculada só aceita aporte manual (flag de transferência lança GoalException); meta `archived`/`completed` rejeita aporte até reabrir (status `active`) (2026-09-03).
- [7.1a] SPEC 4.9 (relatórios 1–4) não define base de data nem status → suposições: (a) coluna segue o `budget_basis` do dono (`date` vs `competence_date`, mesmo padrão do BudgetCalculator); (b) contam `pending` + `cleared` + `reconciled` (só soft-deleted e `is_ignored_in_reports` excluem, coerente com [6.2]); (c) relatório 3 usa receitas = `income` e despesas = `expense` + `credit_card_expense` (transferência, ajuste e pagamento de fatura fora, mesmo critério dos totais [2.3b]); splits só afetam os relatórios 1–2 (o total mensal é o da transação) (2026-09-03).
- [7.1a] SPEC 4.9 item 4 (fluxo de caixa) sem composição definida → suposição: entradas = `income` + `adjustment`, saídas = `expense` + `credit_card_payment` (semântica do AccountBalanceService); `transfer` entre contas próprias e `credit_card_expense` fora; saldo acumulado = Σ saldos iniciais das contas do dono + líquido acumulado; grain `day` preenche dias zerados no intervalo (2026-09-03).
- [7.1a] SPEC 4.9 item 5 sem fórmula ("faturas em aberto + parcelas futuras") → suposição (coluna `date`, semântica caixa 3.1): ativos(M) = Σ saldos iniciais + Σ efeitos em conta `cleared`/`reconciled` com `date` <= fim de M; passivos(M) = Σ `credit_card_expense` (qualquer status) − Σ `credit_card_payment` (`cleared`/`reconciled`) com `date` <= fim de M; patrimônio = ativos − passivos (passivo negativo = crédito a favor); origem de `transfer` assumida própria pelo filtro de `user_id`, destino confirmado por JOIN (conta excluída = sem efeito) (2026-09-03).
- [7.1b] SPEC 4.9 itens 6–10 sem detalhe de composição → suposições: (a) base de data segue o `budget_basis` do dono e contam `pending` + `cleared` + `reconciled` (só soft-deleted e `is_ignored_in_reports` excluem, como em [7.1a]); (b) gastos = `expense` + `credit_card_expense` (`transfer`/`adjustment`/`credit_card_payment` fora); (c) relatório 6 conta só `credit_card_expense` por cartão + mês, com zero-fill para todo cartão do dono no intervalo; (d) payee/tag contam a transação uma vez pelo total mesmo com splits (splits detalham categoria, não payee/tag; tag conta o total em cada etiqueta vinculada); top 20 do payee via `LIMIT 20` (param opcional); (e) relatório 9 lista só budgets `is_active` vigentes no mês (`start_date` <= fim do mês e sem `end_date` anterior ao início), orçado = `amount_cents` independente do `period`, gasto pela regra 3.12 (subcategorias + splits, mesma conta do BudgetCalculator), diferença = orçado − gasto; (f) relatório 10 devolve sempre 12 linhas 01–12 com zeros onde sem movimento (2026-09-03).
- [7.1c] categoria fixa vem na 9.5; até lá só recurrence_id conta como fixa (SPEC 4.9 item 11 cita "ou categoria marcada como fixa", mas `categories` não tem flag) (2026-09-03).
- [7.2a] SPEC 4.9 manda "filtro de período" sem formato por relatório nem forma do drill-down → suposições: (a) datas YYYY-MM-DD nos relatórios 1,2,4,7 e meses YYYY-MM em 3,5,6 (defaults = mês atual / últimos 12 meses); (b) drill-down = expansão inline (1: subcategorias sob o topo com totais próprios sem somar; 6: meses sob o cartão) + link "Ver transações" para a ListTransactions com filtro via query string no formato `tableFilters[{filtro}][value]` (categoria/payee/tipo), sem carregar o período no link; (c) visual em barras CSS puras (mesmo padrão da página de orçamentos 6.3) em vez de ChartWidget, sem pacote novo; (d) hub único na Page Reports existente com seletor dos 7 relatórios em vez de 7 Pages separadas (2026-09-03).
- [7.1c] SPEC 4.9 itens 11–13 sem detalhe → suposições: (a) 11 soma `expense` + `credit_card_expense` no período pela base do `budget_basis`, fixa = `recurrence_id` NOT NULL, splits herdam a flag da mãe, contam pending/cleared/reconciled, retorno `fixed_cents/variable_cents/total_cents`; (b) 12 devolve 90 linhas de `startDate` até +89 dias com `{date, projected_balance_cents}` na coluna `date` (caixa, SPEC 3.1, ignora `budget_basis`), composição = Σ contas via `AccountBalanceService::forecast` por conta com materializadas via mapper + futuras via `ProjectedOccurrences::forAccount` (parcelas/faturas futuras já são `pending`, sem soma dupla); (c) 13 cobre os 3 meses até `referenceMonth` (inclusive) com `{category_id, name, month_totals:[YYYY-MM => cents em ordem cronológica], average_cents=intdiv(soma,3)}`, mesma conta de gastos/splits do núcleo byCategory, ordenado pelo total desc (2026-09-03).
- [7.2b] "link Ver transações por tag se houver filtro de tag, senão link genérico" sem definir filtro de tag no relatório 8 (só há filtro de datas) → suposição: cada linha tem link próprio `tableFilters[tag][value]=id` (mesmo padrão categoria/payee da 7.2a); link genérico só onde não há id (orçado global do relatório 9) (2026-09-03).
- [7.2c] SPEC 4.9 manda "exportação CSV/PDF" sem formato de colunas, valores, nomes de arquivo nem layout → suposições: (a) CSV via league/csv (pacote permitido SPEC 1, já transitivo do Filament, agora dependência direta), delimitador `;`, BOM UTF-8, cabeçalho PT-BR, valores formatados via Money ("R$ 1.234,56"); o fputcsv põe aspas em campos com espaço ("Saldo acumulado"), válido para Excel BR; (b) relatório 1 exporta lista plana por categoria (sem agrupar topos; o drill-down é da tela); (c) relatório 11 inclui linha Total além de Fixas/Variáveis; (d) PDF com layout genérico inline (título + período + tabela, fonte DejaVu, sem CDN/remoto) via barryvdh/laravel-dompdf; (e) nomes `relatorio-NN-slug-periodo.csv/pdf` (período cru YYYY-MM-DD/YYYY-MM nos mesmos formatos da Page); período inválido na exportação vira notificação em vez de download (2026-09-03).
- [7.3] SPEC 4.1/2.16 pedem widgets reordenáveis com "config em user_settings" sem formato do JSON nem fórmulas dos widgets → suposições: (a) `dashboard_widgets` = lista ordenada de `{id, visible}` (ids fixos: total_balance, month_summary, forecast, accounts, cards, upcoming, budgets, categories, cashflow, goals, quick_actions); null = tudo visível na ordem padrão; (b) saldo total = contas ativas com `include_in_dashboard`, variação vs o último dia do mês anterior, % via intdiv (null sem base); (c) cartão = fatura `open` de menor reference_month, dias p/ fechar = closing_date − hoje (negativo = já fechou), limite via CreditCardLimitService; (d) vencimentos em [hoje, hoje+N] com N em {7,15,30} (default 30): faturas não pagas com due na janela + `pending` com recurrence (sem grupo) + `pending` com grupo de parcela; pendente vencida (date < hoje) fica na página "Pendentes", fora do dashboard; (e) visual em barras CSS + donut CSS (conic-gradient) sem ChartWidget/pacote novo, mesmo padrão das páginas 6.3/7.2; (f) atalhos levam ao form unificado de lançamentos (abas por tipo), sem preseleção de aba via URL (2026-09-03).
- [7.5] SPEC 4.1/4.9 item 12 não detalha o widget da projeção → suposições: (a) `DashboardData::balanceProjection90` reutiliza `ReportQuery::balanceProjection` como está (todas as contas do dono, coluna `date` caixa) e só resume `{start_date, end_date, days, final_cents, min_cents, min_date}`; (b) em empate no mínimo, `min_date` é a primeira data; (c) widget `projection_90` ("Projeção 90 dias") visível por padrão ao final (layouts legados ganham o id anexado visível, comportamento genérico do DashboardLayout); (d) visual em barras CSS puras + link `?relatorio=12` para a Page Reports, sem pacote novo (2026-09-03).
- [7.4] SPEC 4.13 lista os eventos sem definir janelas, bordas nem dedupe → suposições: (a) recorrência pendente = transaction `pending` com `recurrence_id` e `date < hoje` (mesmo corte de "vencida" da [7.3]; a de hoje ainda pode ser confirmada no dia); (b) meta concluída = goal `active` com saved (`initial` + aportes via GoalProjectionService) >= target e target > 0 (goals `completed`/`archived` nunca renotificam); (c) janelas inclusivas 0..N dias (`notify_invoice_closing_days_before`, `notify_bills_days_before`; 0 = só hoje); fechamento vale só para `open`, vencimento para `open`/`closed`/`partially_paid`, vencida (`due < hoje`) para qualquer status exceto `paid`; (d) dedupe "uma vez por evento" via type+data na tabela notifications (fechamento/vencimento/vencida por invoice_id+kind, pendência por transaction_id, meta por goal_id), filtrado em PHP para evitar quirks de JSON no SQLite (padrão [6.2]); (e) `ImportFinishedNotification` criada sem trigger (fase 8 não existe); (f) `notify_budget_threshold=false` silencia só orçamento; sem toggle para os demais tipos (só as janelas em dias) (2026-09-03).
- [8.1] SPEC 2.15 lista `status` do import_batch sem enumerar valores nem default → suposição: string livre com default `pending` (o ciclo de vida — processing/completed/failed — é das tasks 8.2/8.3; sem enum fictício) (2026-09-03).
- [8.2a] SPEC 2.8/2.15 não diz se linha deletada libera o external_id → suposição: NÃO libera (Deduplicator consulta com soft-deleted incluído; unique(user_id, external_id) continua bloqueando a reimportação) (2026-09-03).
- [8.2a] SPEC 2.15 não define layouts dos CSVs dos bancos nem o formato do `mapping` → suposição: presets inventariados de headers plausíveis (Nubank: Data;Categoria;Título;Descrição;Valor / Itaú: Data;Lançamento;Agência;Conta;Valor;Saldo / Inter: Data,HISTORICO,Descricao,Valor,Saldo); `mapping` = campos canônicos `date/description/amount` → nome da coluna no arquivo (+ `preset` opcional) (2026-09-03).
- [8.2b] SPEC 2.15 não detalha o OFX (separador NAME+MEMO, TRNTYPE ausente, extrato sem movimento) → suposição: descrição = NAME + MEMO unidos por um espaço (só o existente quando um falta); TRNTYPE ausente vira type `other`; arquivo sem `<STMTTRN>` devolve lista vazia com metadados (não erro) (2026-09-03).
- [8.2a] SPEC 2.15 não lista formatos de data aceitos na importação → suposição: `DD/MM/AAAA`, `AAAA-MM-DD`, `DD-MM-AAAA`, `DD.MM.AAAA` (validação estrita anti-overflow, saída `AAAA-MM-DD`); valores via Money::parse (SPEC 3.15, centavos int, negativo permitido) (2026-09-03).
- [8.3] SPEC 2.8/2.15 não define categoria padrão do lote nem regra de sinal na confirmação → suposição: staging EXIGE `category_id` visível ao usuário (nova coluna `import_batches.category_id`, sem fallback "Outros" automático); destino conta usa o sinal (negativo = expense, positivo = income), destino cartão sempre `credit_card_expense` com abs do valor (fatura CSV tem valores positivos que são compras); lançamentos nascem `cleared` (2026-09-03).
- [8.3] SPEC 2.15 não define o destino de linha com valor zerado nem de erro de parse no OFX → suposição: valor zerado vira row `error` ("nada para importar"); erro de parse no OFX aborta o arquivo inteiro (OfxParser é atômico), só linha CSV inválida vira row `error` sem abortar o lote (2026-09-03).
- [8.4a] SPEC 2.14 não define o formato de `between` para amount → suposição: `min..max` em centavos inteiros, inclusivo nos dois extremos; outro formato nunca casa (2026-09-03).
- [8.4a] SPEC 2.14 não diz como `account`/`credit_card`/`type` casam com `equals` (id ou nome/enum) → suposição: `account`/`credit_card` casam pelo id numérico (string do value); `type` casa pelo value do enum (`expense`, `income`, `transfer`, `credit_card_expense`, `credit_card_payment`, `adjustment`), case-insensitive (2026-09-03).
- [8.4a] SPEC 2.14 lista `set_ignored` sem definir o que é truthy → suposição: truthy = `1`/`true`/`yes`/`sim` (case-insensitive, com trim); qualquer outro valor define `false` (2026-09-03).
- [8.4a] SPEC 2.14/3.9 não diz o que acontece com `set_category` em `transfer` (que nunca tem categoria) → suposição: a ação é ignorada em silêncio, sem quebrar a criação nem as demais ações da regra (2026-09-03).
- [8.4a] SPEC 2.14 não define o comportamento de ação com alvo inexistente/de outro usuário → suposição: ação ignorada em silêncio (sem exceção), demais ações da regra continuam (2026-09-03).
- [8.4a] SPEC 2.14 não cobre regex inválida nem condição de texto com value vazio → suposição: regex inválida nunca casa (sem exceção); condição de texto com value vazio nunca casa (2026-09-03).
- [8.4a] SPEC 2.14 manda disparar "ao criar/importar" sem citar edição → suposição: só CreateTransaction aplica (UpdateTransaction fora de escopo; importação coberta porque ImportConfirmService usa CreateTransaction) (2026-09-03).
- [8.4b] SPEC 2.14/4.11 manda "aplicar regras retroativamente com preview" sem dizer como tratar `reconciled` (3.11 exige desconciliar explícito para editar valor/data) → suposição: `RuleRetroApplier` PULA reconciled (contado em `skipped_reconciled`, sem alterar nada), mesmo padrão das ações em massa que ignoram reconciled [2.6]; as ações de regra nunca tocam valor/data mesmo assim (2026-09-03).
- [8.4b] SPEC não diz se regra inativa aplica retroativamente quando escolhida → suposição: NUNCA (nem explícita: preview/apply devolvem vazio), consistente com o motor que só avalia ativas (2026-09-03).
- [8.5] SPEC 4.10 pede "download do SQLite" sem tratar que o arquivo é único e multi-usuário → suposição: download liberado a qualquer autenticado, com aviso explícito na UI de que o arquivo contém dados de TODOS os usuários do servidor; o formato por-usuário é o backup JSON (2026-09-03).
- [8.5] SPEC 4.10 manda "restauração de backup JSON" sem dizer se soma ou substitui → suposição: SUBSTITUI (wipe dos dados do usuário + import numa única DB::transaction), o que torna o restore idempotente (rodar 2x = mesmo estado) e o round-trip export→restore fiel; CSV é só leitura e nunca restaura (2026-09-03).
- [8.5] SPEC não define o que entra no backup (apagadas? sistema? settings?) → suposições: só linhas vivas (soft-deleted fora); categorias do sistema (user_id null) e bancos globais fora (são referência global, preservados no wipe, ids seguem válidos); `user_settings` via upsert (a conta continua a mesma); `import_batches`/`import_rows`, `activity_log` e anexos fora (staging/auditoria, não entidades do backup); taggables restritos ao morph de Transaction (2026-09-03).
- [8.5] Restore recria linhas com os ids originais via `DB::table` (sem Eloquent de propósito: o wipe passaria pelo guard 3.13 e o insert dispararia observers/activitylog — restore é reposição de dados, não ação de domínio, lição [6.1]); ordem topológica pais→filhos no insert e inversa no wipe, sem desligar FKs (2026-09-03).
- [8.5] SPEC 4.10 não define nomes de arquivo nem colunas do CSV por entidade → suposições: nomes determinísticos `{slug}.csv` (ex.: `transacoes.csv`), `backup-finance-iam.json` e `database.sqlite`; CSV com `;` + BOM + cabeçalho PT-BR + Money formatado (padrão 7.2c); coluna "Etiquetas" em `transacoes.csv` com nomes separados por vírgula (informativa); splits/aportes/condições-ações/períodos em arquivos próprios (2026-09-03).
- [8.6] SPEC 5 lista `invoices` como endpoint sem dizer quais operações → suposição: leitura (index/show) + `POST /invoices/{id}/pay` (via PayInvoice, regra 3.5); SEM store/update/destroy (fatura nasce lazy pelo domínio e muda de status pelo job/pagamento; criar ou editar à mão quebraria alocação e totais) (2026-09-04).
- [8.6] SPEC 5 lista `installments` sem detalhar → suposição: o recurso é o `installment_groups` (index/show/store via CreateInstallmentPlan + destroy via CancelInstallmentPlan, `?delete_all=true` apaga tudo); SEM update direto (antecipar/editar futuras continua exclusivo da UI) (2026-09-04).
- [8.6] SPEC 5 não detalha update de recorrência → suposição: update limitado a `name` (edição direta segura) + `is_active` (pausa/retomada via PauseRecurrence, regra 3.8); a edição em cascata do template ("esta/futuras/todas") continua exclusiva da UI (2026-09-04).
- [8.6] SPEC 5 não define o período default da listagem de lançamentos → suposição: SEM filtro default (devolve tudo paginado); o consumidor filtra com os mesmos critérios da 4.2 (`date_from/date_to`, `account_id`, `credit_card_id`, `category_id` incl. splits, `tag_id`, `payee_id`, `type`, `status`, `min_cents/max_cents`, `search` em descrição + observações) (2026-09-04).
- [8.6] SPEC 5 pede "resources com `amount` em centavos + `amount_formatted`" → suposição: todo valor sai em trio (`{nome}_cents` + `{nome}` em centavos + `{nome}_formatted` em "R$ 1.234,56" via Money); relatórios acrescentam `*_formatted` sem remover os centavos (2026-09-04).
- [9.1] SPEC 2.2 permite cache de saldo ("pode existir cache invalidado por observer") sem definir colunas, frescor nem cobertura → suposições: (a) `cached_balance_cents` (nullable) + `cached_balance_at` (nullable), null = stale; (b) fresco = cache do dia atual America/Sao_Paulo (o corte `date <= hoje` move sem escrita, então virar o dia invalida por tempo); (c) TransactionObserver cobre create/update/delete/forceDelete/restore via Eloquent; `PauseRecurrence` (delete em massa por query builder) remove só `pending` futuras, que não compõem o saldo atual, sem invalidação necessária; (d) `ReallocateCategory` (update em massa de `category_id`) não afeta saldo (categoria não entra na fórmula 3.1); (e) escrita do próprio cache via query builder (sem eventos, sem loop); colunas fora do fillable (gerenciadas pelo sistema) (2026-09-04).
- [9.1] Índices além da SPEC 2.8 sem lista fechada → suposição: índice explícito em TODA FK (foreignId constrained não cria índice) + `competence_date` (base alternativa dos relatórios) + compostos dos filtros quentes (`transactions(user_id,status,date)`, `invoices(user_id,due_date)`, `invoices(credit_card_id,status)`); sem remover os existentes (2026-09-04).
- [9.1] EXPLAIN sem definição de "tabela grande" → suposição: exigência vale para `transactions`, `transaction_splits` e `taggables` (qualquer `SCAN <tabela>` reprova); tabelas de dimensão (categories, payees, tags, budgets, invoices, accounts) podem varrer (2026-09-04).
- [9.4] SPEC pede a atualização manual via "adjustment `cleared`/`reconciled`" sem fixar status, descrição, status do aporte/resgate nem validação do novo saldo → suposições: ajuste manual nasce `cleared` (a reconciliação posterior continua possível); descrição "Atualização de saldo do investimento"; categoria reutiliza "Ajuste de saldo" via `ReconciliationService::ADJUSTMENT_CATEGORY` (sem duplicar string); aporte/resgate nascem como transfer `cleared` (dinheiro saiu/entrou de fato); novo saldo precisa ser >= 0 (negativo lança AccountException); descrições default "Aporte {conta}"/"Resgate {conta}" (2026-09-04).
- [9.5] SPEC 4.9 item 11 ("ou categoria marcada como fixa") sem granularidade para splits → suposição: granularidade por split — cada split é fixo se a mãe tem `recurrence_id` OU a categoria DO SPLIT é fixa (direta: mãe com `recurrence_id` OU categoria da transação fixa); mãe com recorrência fixa todos os splits mesmo com categoria variável (2026-09-04).
- [9.5] Task lista fixas "óbvias" sem mapeamento exato ao catálogo → suposição: "Plano de saúde" = `Plano` (Saúde); fixas = Aluguel, Condomínio, Energia, Água, Internet, Plano, Assinaturas (topo), Streaming; `Gás` fica variável (consumo varia, fora da lista) (2026-09-04).
- [9.6] SPEC/task pede wizard com "opção de pular e de retomar" sem definir persistência → suposição: sem coluna nova; "precisa de onboarding" = zero contas do usuário; pular = sair da página (link para o dashboard); o item de menu "Começar agora" só é registrado enquanto precisa (`shouldRegisterNavigation`), mas a URL continua acessível a qualquer autenticado (estado "concluído", sem bloquear ninguém) (2026-09-04).
- [9.6] DemoSeeder "idempotente (verificar por email + flag)" sem formato da flag → suposição: email `demo@example.com` via `firstOrCreate` + early-return quando o demo já tem transações; contas/cartão/payees via `firstOrCreate(user_id + nome)` para não duplicar nem quebrar se as transações forem apagadas (2026-09-04).
- [9.6] DemoSeeder "12 meses" sem janela definida → suposição: dos 11 meses atrás até hoje (datas reais relativas ao dia atual); lançamentos com data futura no mês corrente nascem `pending`, o resto `cleared`; recorrência e parcelamento criados via domínio (`CreateRecurrence`/`CreateInstallmentPlan`) para exercitar faturas lazy (2026-09-04).
