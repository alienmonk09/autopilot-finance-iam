Review #1

## Resultado do check

Comando executado: `bash scripts/check.sh` (literal, sem pipe/tail por restrição de permissão do ambiente).

Saída literal:

```json
{"tool":"pint","result":"passed"}
{"tool":"phpstan","result":"passed","errors":0}
{"tool":"pest","result":"passed","tests":318,"passed":318,"assertions":1328,"duration_ms":5970}
CHECK: PASS
```

318/318 testes verdes. Fase 2 tem 2.1, 2.2, 2.3a, 2.3b, 2.3c, 2.4, 2.5, 2.6, 2.7 todas `[x]`; nenhuma `[!]`/`[-]` — nada a abater do veredito.

## Aceite da fase 2 (item → OK/FALHA + evidência)

Aceite ROADMAP: "lançar despesa/receita/transferência/ajuste com status, split, anexo, tags; filtros e totais corretos; auditoria gravada".

| Item do aceite | Resultado | Teste que prova (arquivo:linha) | Código que implementa (arquivo:linha) |
|---|---|---|---|
| Lançar despesa | OK | `tests/Feature/Transactions/TransactionActionsTest.php:31` ("cria despesa com valor int em centavos"); `tests/Feature/Filament/TransactionResourceTest.php:131` (criar despesa pelo painel grava centavos via domínio) | `app/Domain/Transactions/CreateTransaction.php:31` (`create()`); `app/Domain/Transactions/TransactionValidator.php:62` (rejeita float) |
| Lançar receita | OK | `tests/Feature/Transactions/TransactionActionsTest.php:343` (status pending/cleared/reconciled com `type income`); `tests/Feature/Filament/TransactionResourceTest.php:270` ("criar receita e ajuste pelas abas grava o tipo certo"); `tests/Feature/Filament/QuickCreateTransactionTest.php:90` (receita pelo modal) | `app/Domain/Transactions/CreateTransaction.php:61-76` (ramo não-transfer, categoria obrigatória); `app/Filament/App/Resources/TransactionResource.php:411-431` (Tabs Despesa·Receita·Transferência·Ajuste) |
| Lançar transferência (linha única) | OK | `tests/Feature/Transactions/TransactionActionsTest.php:90` (linha única origem+destino); `:112` (afeta as duas contas com sinais opostos); `tests/Feature/Filament/TransactionResourceTest.php:162` (transferência pelo painel sem categoria); `tests/Feature/Filament/QuickCreateTransactionTest.php:126` (transfer pelo modal) | `app/Domain/Transactions/TransferService.php:26` (`create()` força `type=transfer`); `:32-52` (`validateAccounts`, origem≠destino); `:55-64` (sem categoria/splits) |
| Lançar ajuste | OK | `tests/Feature/Transactions/TransactionActionsTest.php:232` (`income/expense/adjustment` sem splits exigem categoria); `tests/Feature/Filament/TransactionResourceTest.php:270` (aba adjustment grava tipo); `tests/Feature/Accounts/ReconciliationTest.php:89` (diferença positiva cria `adjustment` reconciled; negativa cria `expense` "Ajuste de saldo") | `app/Domain/Transactions/CreateTransaction.php:65-72` (adjustment exige conta+categoria); `app/Domain/Accounts/ReconciliationService.php:175-206` (`createAdjustment`, `adjustment` soma / `expense` corrige p/ baixo) |
| Status (pending/cleared/reconciled, default pending) | OK | `tests/Feature/Transactions/TransactionActionsTest.php:343` (3 status aceitos, outro rejeitado); `:370` ("sem status informado nasce pendente") | `app/Domain/Transactions/CreateTransaction.php:35-37` (default `Pending`); `app/Domain/Transactions/TransactionValidator.php` (`status()`); `app/Enums/TransactionStatus.php` |
| Split (Σ splits == amount, category nulo) | OK | `tests/Feature/Transactions/TransactionActionsTest.php:183` (soma equivale, category nulo); `:204` (soma divergente não persiste nada); `:220` (splits+category_id rejeitado); `tests/Feature/Filament/TransactionResourceTest.php:214` + `:245` (via painel, soma válida/inválida); `tests/Feature/Filament/QuickCreateTransactionTest.php:156,191` (via modal) | `app/Domain/Transactions/SplitValidator.php:18-55` (validação soma exata, `TransactionException`); `app/Domain/Transactions/CreateTransaction.php:75` + `:104-106` (valida e persiste em `DB::transaction`) |
| Anexo (comprovante) | OK | `tests/Feature/Transactions/TransactionSchemaTest.php:232` ("transação aceita anexo via medialibrary", `Storage::fake`, `toMediaCollection('attachments')`); form com upload coberto por `tests/Feature/Filament/TransactionResourceTest.php:131` (criação pelo painel) | `app/Models/Transaction.php:21-22,36` (`InteractsWithMedia`, `HasMedia`); `app/Filament/App/Resources/TransactionResource.php:583-590` (`FileUpload attachments`, disk public); `:378-390` (`attachUploads()` para coleção `attachments`) |
| Tags | OK | `tests/Feature/Transactions/TransactionActionsTest.php:323` ("cria lançamento com payee e tags"); `tests/Feature/Transactions/TransactionSchemaTest.php:219` (tags via taggables); `tests/Feature/Filament/TransactionResourceTest.php:308` (payee+tags pelo painel) | `app/Domain/Transactions/CreateTransaction.php:79,108-110` (`tagIds` + `sync` em transaction); `app/Filament/App/Resources/TransactionResource.php:519-526` (`Select tag_ids multiple`) |
| Filtros (4.2) corretos | OK | `tests/Feature/Filament/TransactionFiltersTest.php:90` (9 filtros existem: periodo, conta, categoria, tag, payee, tipo, status, valor, texto); `:101` (conta mostra origem+destino da transfer); `:116` (categoria inclui via splits); `:126,136,146,161,171,186,201,223,240,254,277` (tag/payee/tipo/status/valor-BR/texto/presets hoje, semana, mês anterior, ano, personalizado); `:329` (agrupamento por dia `d/m/Y`); `:346` (sem vazamento entre usuários) | `app/Filament/App/Resources/TransactionResource.php:669-860` (filtros periodo/conta/categoria/tag/payee/tipo/status/valor/texto); `:154-168` (`periodRange`); `:707-725` (conta = `account_id OR transfer_account_id`, SPEC 3.9); `:731-744` (categoria direta OU via splits) |
| Totais do filtro no rodapé corretos | OK | `tests/Feature/Filament/TransactionFiltersTest.php:290` ("rodapé soma receitas despesas e resultado ignorando transferência": R$ 5.000,00 / R$ 2.178,70 / R$ 2.821,30); `:305` (rodapé respeita filtro ativo); `:318` (totais via query sem carregar coleção); `tests/Unit/Domain/Transactions/TransactionTotalsTest.php:21,36` (`fromRows`, transfer/adjustment ignorados) | `app/Domain/Transactions/TransactionTotals.php:34-54` (`fromRows`: income soma, expense+credit_card_expense somam, resto ignora); `:63-86` (`summarize()` agrega por query) |
| Auditoria gravada (2.18) | OK | `tests/Feature/Transactions/TransactionActionsTest.php:480` (create/update/delete gravam `Activity`); `tests/Feature/Transactions/TransactionSchemaTest.php:249` (factory create/update logam); `tests/Feature/Transactions/BulkTransactionActionsTest.php:213` (massa gera update+delete); `tests/Feature/Accounts/ReconciliationTest.php:233` (reconciliação gera update+create) | `app/Models/Transaction.php:19,36` (`LogsActivity` trait); `app/Domain/Accounts/ReconciliationService.php:91-92,125-126` (save → activitylog do model); bulk via `BulkTransactionActions` + `DeleteTransaction` |
| Extras da fase: autocomplete payee→categoria (2.4) | OK | `tests/Feature/Payees/PayeeCategorySuggestionTest.php:30,45` (padrão / última usada); `:97` (splits usa maior valor); `:148,166,206,230,251,273` (aprende no create/update, não aprende em transfer, preenche form sem sobrescrever manual) | `app/Domain/Payees/PayeeCategorySuggestion.php` (`suggest()`/`learn()`); `app/Filament/App/Resources/TransactionResource.php:268-300` (`suggestCategory()`); `app/Domain/Transactions/CreateTransaction.php:112-114` (learn, exceto transfer) |
| Extras da fase: extrato com acumulado (2.5) | OK | `tests/Feature/Accounts/AccountStatementTest.php:79` (ordem date+id, acumulado linha a linha, ajuste soma, cartão 0); `:108` (transfer sinais opostos); `:123` (pending sinalizado sem mover acumulado); `tests/Feature/Filament/ViewAccountTest.php:65` (saldos + acumulado + Pendente na UI) | `app/Domain/Accounts/AccountStatementService.php:28` + `app/Domain/Accounts/StatementLine.php:18` (centavos, pending não move acumulado); `app/Domain/Accounts/TransactionBalanceMapper.php` |
| Extras da fase: reconciliação UI (2.6) | OK | `tests/Feature/Accounts/ReconciliationTest.php:39,51,89,138,150,175,202` (preview, marca só cleared≤D, ajuste ±, idempotente, desconciliar, bloqueio edição/exclusão de reconciled, lote ignora reconciled); `tests/Feature/Filament/ReconcileAccountTest.php:38,58,72` (modal reconciliar/desconciliar, sem ajuste quando diferença zero) | `app/Domain/Accounts/ReconciliationService.php:48-63` (`preview`), `:68-102` (`reconcile`), `:109-132` (`unreconcile`), tudo em `DB::transaction` |
| Extras da fase: ações em massa (2.3c) | OK | `tests/Feature/Transactions/BulkTransactionActionsTest.php:31-230` (categorizar limpa splits, ignora transfer, marcar cleared nunca toca reconciled, soft delete, mover conta, auditoria); `tests/Feature/Filament/TransactionBulkActionsTest.php:42-133` (5 ações existem na tabela e operam) | `app/Domain/Transactions/BulkTransactionActions.php` (domínio); `app/Filament/App/Resources/TransactionResource.php:891-970` (bulk actions só plugam UI no domínio via `handleBulk()` `:981-1011`) |
| Extras da fase: navegação mês + modal rápido (2.3a, 2.7) | OK | `tests/Feature/Filament/TransactionResourceTest.php:52,77,113` (mês corrente default, ‹ › com rótulo pt-BR, virada de ano); `tests/Feature/Filament/QuickCreateTransactionTest.php:23,44,62,90,126,156` (botão topbar + FAB, modal cria despesa/receita/ajuste/transfer/split) | `app/Filament/App/Resources/Pages/ListTransactions.php` (props `mes/ano`, `previousMonth/nextMonth/goToCurrentMonth`); `app/Livewire/QuickCreateTransaction.php` + `app/Filament/App/Resources/TransactionResource.php:405` (`formComponents()` fonte única painel+modal) |

Nenhum item do aceite FALHOU. Observação (não-FALHA): anexo é provado no nível do model/medialibrary (`TransactionSchemaTest.php:232`) + wiring do `FileUpload` no form; não há teste Livewire de upload de arquivo ponta a ponta — aceitável para a fase, fica como melhoria opcional futura, não corretiva.

## Regras da seção 3 tocadas (regra → OK/FALHA + teste)

| Regra | Resultado | Teste que prova |
|---|---|---|
| 3.9 Transferência single-row, sinais opostos, fora de receita/despesa | OK | `TransactionActionsTest.php:90` (linha única), `:112` (sinais opostos via `AccountBalanceService`), `:142,155` (mesma conta / sem destino / com categoria ou splits rejeitados), `TransactionFiltersTest.php:101` (filtro conta vê os dois lados), `TransactionFiltersTest.php:290` + `TransactionTotalsTest.php:21` (transfer ignorada nos totais), `AccountStatementTest.php:108` (extratos opostos) |
| 3.10 Split Σ==amount, category nulo, relatórios usam splits | OK | `TransactionActionsTest.php:183,204,220,248` (soma exata, atomicidade, category nulo, categoria alheia/zero rejeitados), `TransactionFiltersTest.php:116` (filtro categoria acha via splits), `BulkTransactionActionsTest.php:31` (categorizar limpa splits) |
| 3.11 Reconciliação (preview, marca cleared→reconciled, ajuste, desconciliar, bloqueio edição) | OK | `ReconciliationTest.php:39` (preview diferença), `:51` (marca só cleared≤D, ignora pendente/futura/outra conta), `:89` (ajuste ± com categoria "Ajuste de saldo"), `:138` (idempotente), `:150` (desconciliar), `:175` (editar/excluir reconciled lança "desconciliar"), `:202` (lote ignora reconciled), `BulkTransactionActionsTest.php:134` (markCleared nunca toca reconciled), `ReconcileAccountTest.php:38,58,72` (UI) |
| 3.15 Dinheiro em centavos, nunca float, parse BR/US | OK | `TransactionActionsTest.php:31` (int centavos), `:50` (`"1.234,56"` e `"1234.56"` → 123456), `:70` (float/zero/negativo/texto inválido rejeitados, nada persiste), `TransactionResourceTest.php:131` + `QuickCreateTransactionTest.php:62` (painel/modal `"1.234,56"` → 123456), `MoneyTest` (parse/formatação/soma/alocação — fase 0, base) |
| 3.1 Saldo (só cleared/reconciled contam; pending não move acumulado) — tocada via extrato | OK | `AccountStatementTest.php:123` (pending sinalizado, acumulado inalterado), `:79` (acumulado linha a linha fecha com `AccountBalanceService`), `:171` (abertura carrega saldo anterior), `ViewAccountTest.php:65` (saldo atual 519410 vs previsto 495954 na UI) |
| 3.14 Datas DATE puro / timezone — tocada via filtros, competência e extrato | OK | `TransactionActionsTest.php:31` (`competence_date` default = `date`), `TransactionSchemaTest.php:170` (competence assume date), `TransactionFiltersTest.php:201-288` (presets hoje/semana/mês anterior/ano/personalizado com `whereDate`), `ReconciliationService.php:217-220` (`normalizeDate` Y-m-d) |

Regras 3.2–3.8 (cartão, fatura, limite, pagamento, fechamento, parcelamento, recorrência) e 3.12–3.13 (orçamento, exclusão com transações) pertencem às fases 1/3+ — fora do escopo desta auditoria.

## Sinais de contorno encontrados

- `skip` / teste pulado: **nenhum**. Grep por `skip\(|markTestSkipped` em `tests/` retorna zero matches.
- Asserção trivial: **1 ocorrência, fora do escopo da fase** — `tests/Unit/ExampleTest.php:14` (`$this->assertTrue(true)`, scaffold padrão do Laravel). Nenhum teste da fase 2 usa asserção trivial; todos assertam valores, contagens, status e eventos de auditoria.
- Código de produção citando mock/teste: **nenhum**. Grep por `TODO|FIXME|mock|Mock` em `app/` retorna zero matches.
- Float em dinheiro: **sem contorno**. Persistência é sempre `amount_cents` int (`CreateTransaction.php:38`, `SplitValidator.php:43-46`, `TransactionTotals.php:44`). `TransactionValidator.php:58,62-63` **rejeita** `is_float` explicitamente. `Money.php:55` (`toFloat()`, documentado "só para exibição/cálculo externo, nunca persistir") e `Money.php:93-99` (`multiply(int|float)` com `round(..., HALF_UP)` para int) são uso auxiliar legítimo, não armazenamento.
- Lógica de negócio em Resource/Controller/Livewire: **sem contorno**. `TransactionResource.php:68` documenta "criação/edição/exclusão passam pelo domínio nas páginas — nunca Eloquent direto"; delete chama `DeleteTransaction` (`:877-889`), bulks chamam `BulkTransactionActions` (`:905-969` via `handleBulk()` `:981-1011`), create/edit via `Create/UpdateTransaction` nas Pages. Helpers no Resource (`formatAmount` `:313`, `typeColor` `:325`, `periodRange` `:154`, `parseFilterCents` `:210`, `suggestCategory` `:268`, `toDomainPayload` `:349`, `attachUploads` `:378`) são apresentação/tradução form→domínio, e a validação real (ownership, soma de splits, origem≠destino) mora em `CreateTransaction`/`SplitValidator`/`TransferService`/`TransactionValidator`.
- `TODO` sem entrada no ROADMAP: **nenhum** (grep zero em `app/`).

## Gaps e tasks corretivas sugeridas

Nenhuma FALHA encontrada — nenhuma task corretiva `2.Fx` a criar.

- Cobertura observada (não-corretiva, melhoria opcional futura): teste Livewire de upload de anexo ponta a ponta no form (hoje coberto no model em `TransactionSchemaTest.php:232` + wiring em `TransactionResource.php:583-590,378-390`).

Veredito: APROVADA
