Review #1



# Review da fase 8 — Importação, Exportação, Regras, API

Aceite da fase (ROADMAP): "importar CSV Nubank/Itaú/Inter de exemplo e OFX sem duplicar em reimportação; regras aplicadas na importação e retroativamente com preview; backup/restore round-trip; API com testes de autenticação e filtros". Tasks `[!]`/`[-]` na fase: nenhuma (8.1 a 8.6 todas `[x]`).

## Resultado do check

Saída literal de `scripts/check.sh` (rodado pelo reviewer):

```
{"tool":"pint","result":"passed"}{"tool":"phpstan","result":"passed","errors":0}
{"tool":"pest","result":"passed","tests":962,"passed":962,"assertions":4385,"duration_ms":49302}
CHECK: PASS
```

## Aceite da fase (item → OK/FALHA + evidência)

1. **Importar CSV Nubank/Itaú/Inter de exemplo e OFX sem duplicar em reimportação → OK**
   - Implementação: `app/Domain/Imports/CsvParser.php:25` (autodetect `; , TAB |`, UTF-8/Latin-1, BOM), `app/Domain/Imports/ImportMapper.php:37` (presets `nubank`/`itau`/`inter` + `mappingForPreset:78`, `mapRow:110` via `Money::parse`, datas `DD/MM/AAAA` e cia. com round-trip anti-overflow `:200`), `app/Domain/Imports/OfxParser.php:23` (SGML 1.x + XML 2.x, `DTPOSTED` só `AAAAMMDD:81`, `TRNAMT` via `Money::parse:113`), `app/Domain/Imports/Deduplicator.php:42` (`external_id` = sha256 data|centavos|descrição normalizada|dono, consulta inclui soft-deleted `:57`), staging em `app/Domain/Imports/ImportStagingService.php:47` (`stageCsv`) e `:92` (`stageOfx`), preview + confirmação idempotente em `app/Domain/Imports/ImportConfirmService.php:44` (`preview`) e `:64` (`confirm`, só rows `pending`, `DB::transaction`, `seen` intra-lote `:71`), wizard Filament em `app/Filament/App/Pages/Import.php` (staging → mapping → preview → confirmar).
   - Testes (fixtures PT-BR em `tests/Fixtures/imports/`: `nubank-fatura.csv`, `itau-extrato.csv`, `inter-extrato.csv`, `itau-extrato.ofx`):
     - CSV/parser+mapping+dedupe: `tests/Feature/Imports/CsvImportTest.php:46` (`;` Nubank, 8 linhas), `:62` (`,` Inter), `:73` (TAB e pipe), `:83` (Latin-1), `:97` (BOM), `:117` (preset detectado nos 3 bancos), `:128` (mapeamento canônico dos 3), `:138` (linha Nubank `1.800,00` → 180000), `:147` (linha Inter ISO negativa → −3250), `:189` (hash estável normalizando acento/caixa), `:210` (soft-deleted continua bloqueando, isolamento), `:245` (reimportação Nubank 8 → 0).
     - OFX: `tests/Feature/Imports/OfxImportTest.php:52` (6 registros, primeiro exato, BANKID/ACCTID), `:81` (hora/timezone ignorados), `:96` (overflow `20260231` rejeitado), `:102` (centavos exatos com sinal, sem float), `:152` (OFX 2.x XML), `:181` (Latin-1 com acentos), `:207` (reimportação 6 → 0), `:241` (hash isola conta/cartão/usuário).
     - Wizard fim a fim: `tests/Feature/Imports/ImportWizardTest.php:57` (staging Nubank → 8 `pending` com parsed canônico), `:85` (staging OFX → 6), `:108` (mapping salvo e reusado), `:138` (linha inválida vira `error` sem abortar), `:158` (preview marca duplicada sem persistir), `:188` (confirm cria 5 expense + 1 income, reconfirmar cria 0), `:232` (cartão vira `credit_card_expense`, reimportação 0 novas + 8 `duplicate`), `:320` (página Filament confirma pelo botão), schema/isolamento/policies/cascades em `tests/Feature/Imports/ImportRuleSchemaTest.php:79-441`.
   - Ressalva não-bloqueante (sem FALHA, ver Gaps): o caminho staging→confirm fim a fim é exercido com Nubank CSV e OFX; Itaú/Inter CSV são provados no nível parser/preset/mapeamento (`CsvImportTest.php:117,128,147`), e o pipeline de staging é genérico sobre o mapping — cobertura suficiente para o aceite, com teste fim a fim dos outros dois formatos como melhoria opcional.
2. **Regras aplicadas na importação e retroativamente com preview → OK**
   - Implementação: `app/Domain/Rules/RuleEngine.php:44` (`apply`, prioridade asc + `stop_processing`, regra sem condições nunca casa, `set_category` em `transfer` ignorado `:296`, alvo alheio/inexistente ignorado, centavos int), hook automático em criação/importação via `CreateTransaction` (a importação usa `ImportConfirmService` → `CreateTransaction`), `app/Domain/Rules/RuleRetroApplier.php:50` (`preview` dry-run com rollback, lista antes/depois + tags) e `:72` (`apply` persiste exatamente o preview em `DB::transaction`), `reconciled` pulado e contado (`candidates:121`, `countReconciled:132`), UI builder + preview/apply em `app/Filament/App/Pages/Rules.php` (só orquestra o Domain).
   - Testes: `tests/Feature/Rules/RuleEngineTest.php:105` (contains case-insensitive + `set_category`), `:146` (regex; inválida nunca casa), `:165` (`gt/lt/between` em centavos), `:257` (AND vs OR), `:297` (prioridade + stop), `:423` (`set_category` em transfer ignorado), `:445` (importação aplica regras via `ImportConfirmService`); `tests/Feature/Rules/RuleRetroApplierTest.php:92` (dry-run sem persistir), `:121` (apply == preview, segunda passada vazia), `:142` (stop vale no retroativo), `:177` (tag soma sem remover), `:260` (transfer sem categoria mas com notes), `:290` (reconciled pulado/contado), `:311` (preview de regra única); UI em `tests/Feature/Filament/RulesPageTest.php:67` (builder grava regra+condição+ação), `:119` (pré-visualizar sem persistir), `:140` (aplicar persiste com contagem), `:160` (aplicar todas).
3. **Backup/restore round-trip (+ CSV por entidade, SQLite) → OK**
   - Implementação: `app/Domain/Exports/EntityCsvExporter.php:81` (17 entidades, `;` + BOM + cabeçalho PT-BR + `Money::format`, `transactions` com coluna Etiquetas e splits em arquivo próprio), `app/Domain/Exports/BackupService.php:119` (`export` versionado `{version, exported_at, user_id, data}`, 20 entidades, só linhas vivas, globais fora) e `:199` (`restore` wipe+import atômico substitutivo = idempotente, ordem topológica, `DB::table` sem observers, validação rígida de dono `:250`), `sqlitePath:240`, Page `app/Filament/App/Pages/Backup.php` (só orquestra: exportar CSV, baixar JSON/SQLite, restaurar via conteúdo).
   - Testes: `tests/Feature/Exports/EntityBackupTest.php:168` (BOM/`;`/PT-BR/`Money`), `:190` (17 entidades com linhas + isolamento total por usuário), `:221` (versionado, 20 entidades, 7 lançamentos/2 splits/1 tag, sem vazar o Bruno), `:257` (round-trip: `export→restore→export` byte-igual, splits/tags preservados), `:283` (idempotente, aceita string JSON), `:295` (remove extra, rejeita linha de outro dono sem apagar), `:321` (payload inválido não apaga nada), `:348` (página baixa CSV/JSON e restaura 7 lançamentos), `:377` (JSON inválido avisa sem quebrar; SQLite conforme ambiente).
4. **API com testes de autenticação e filtros (+ OpenAPI) → OK**
   - Implementação: `routes/api.php:29-31` (`POST /tokens` com `throttle:10,1`, resto sob `auth:sanctum` + `throttle:api`), controllers em `app/Http/Controllers/Api/V1/` delegando mutações ao Domain (`TransactionController.php:53-64` usa `Create/Update/DeleteTransaction`; filtros de query só montam `where`: `filtered:110` — conta pega origem+destino `:122`, categoria inclui splits `:139`, texto em descrição+notas `:163`), Form Requests, Resources com trio `{x}_cents`/`{x}`/`{x}_formatted`, paginação + `meta.totals` (`TransactionController.php:33-51`), OpenAPI via Scramble (`/api/docs/openapi.json`).
   - Testes: `tests/Feature/Api/ApiAuthTest.php:20` (emite token), `:38` (credencial ruim → 422), `:51` (sem token → 401 nas 4 rotas), `:58` (outro usuário vê `total` 0), `:78` (revogação invalida); `tests/Feature/Api/ApiTransactionsTest.php:31` (centavos + `R$ 55,90`), `:54` (`"1.234,56"` → 123456), `:67` (float/negativo → 422), `:101` (período+tipo), `:122` (conta pega transferência origem+destino, 3.9), `:142` (categoria via splits, 3.10), `:162` (tag/payee/status/faixa/texto), `:197` (cartão), `:213` (totais do filtro); `tests/Feature/Api/ApiCrudTest.php:24-232` (CRUD contas/cartões/faturas+pagamento/categorias-tags-payees/recorrências/parcelamentos/orçamentos/metas); `tests/Feature/Api/ApiReportsTest.php:31-57` (summary/by-category/cash-flow com valores exatos), `:67` (OpenAPI 3.1 com os 14 paths + `/api/docs` OK).

## Regras da seção 3 tocadas (regra → OK/FALHA + teste)

- **Regra 3.5 (pagamento de fatura) → OK.** Tocado via `POST /invoices/{id}/pay` (PayInvoice). Prova: `tests/Feature/Api/ApiCrudTest.php:75` (faturas: lista, detalhe e pagamento via API).
- **Regra 3.9 (transferência sem categoria, fora de receita/despesa) → OK.** `RuleEngine` ignora `set_category` em `transfer`; filtro de conta da API pega origem e destino. Provas: `tests/Feature/Rules/RuleEngineTest.php:423`, `tests/Feature/Rules/RuleRetroApplierTest.php:260`, `tests/Feature/Api/ApiTransactionsTest.php:122`.
- **Regra 3.10 (splits) → OK.** Filtro de categoria inclui lançamentos via splits; backup leva splits. Provas: `tests/Feature/Api/ApiTransactionsTest.php:142`, `tests/Feature/Exports/EntityBackupTest.php:257` (2 splits preservados no round-trip).
- **Regra 3.11 (reconciliação) → OK.** Aplicação retroativa pula `reconciled` (exige desconciliar explícito) e conta à parte. Prova: `tests/Feature/Rules/RuleRetroApplierTest.php:290`.
- **Regra 3.13 (exclusão/arquivamento) → OK (reutilizada, sem comportamento novo).** Fase 8 só acrescenta `nullOnDelete`/`cascade` do grafo de importação/regras. Provas: `tests/Feature/Imports/ImportRuleSchemaTest.php:369` (lote→linhas cascade + transações desvinculadas), `:382`, `:398`, `:418`, `:430` (conta/cartão anulam alvo do lote), `:441` (LGPD).
- **Regra 3.14 (datas/timezone) → OK.** Parsing estrito com round-trip anti-overflow em `America/Sao_Paulo` nos dois parsers. Provas: `tests/Feature/Imports/OfxImportTest.php:96` (`20260231` rejeitado), `tests/Feature/Imports/CsvImportTest.php:156` (`32/13/2026` rejeitado), `tests/Feature/Imports/CsvImportTest.php:138` (saída `AAAA-MM-DD`).
- **Regra 3.15 (dinheiro, nunca float) → OK.** Tudo via `Money::parse`/`fromCents`; API rejeita float. Provas: `tests/Feature/Api/ApiTransactionsTest.php:31,54,67`, `tests/Feature/Imports/OfxImportTest.php:102`, `tests/Feature/Imports/CsvImportTest.php:138,147`.
- **Nenhuma outra regra nova da seção 3 nesta fase — declarado explicitamente.** Importações criam lançamentos `cleared` via `CreateTransaction` (respeitando 3.1/3.9/3.10 por construção, sem fórmula nova); regras nunca tocam valor/data (só categoria/payee/descrição/notas/ignorado/tags — `RuleRetroApplier.php:41`); 3.2–3.4, 3.6–3.8, 3.12 sem comportamento novo. SPEC 2.18 (auditoria de `rules`): trait `LogsActivity` presente em `app/Models/Rule.php:29`, mas sem teste que afirme linhas de `activity_log` para regras — ressalva opcional abaixo, não FALHA (fora do aceite e fora da seção 3).

## Sinais de contorno encontrados

- **Teste `skip`: nenhum.** O único `skip` no repo é chamada de domínio legítima (`ResolvePendingOccurrenceTest.php:133`, `$this->resolver->skip(...)`, fase 5). Nenhum `markTestSkipped`/`->skip` nos testes da fase 8.
- **Asserção trivial: nenhuma.** Asserções conferem valores exatos (centavos, hashes `^[0-9a-f]{64}$`, contagens 8/6/7, `toBe($backup['data'])` byte-igual no round-trip, `%PDF`-equivalentes BOM/`%`, igualdade preview==apply, `meta.total`/`totals`).
- **Código de produção citando mock/teste: nenhum.** Grep por `mock|Mock|fake|Fake` em `Imports`/`Rules`/`Exports` retorna vazio (o único "float" é o comentário "nunca float" em `EntityCsvData.php:11`).
- **Float em dinheiro: nenhum.** `Money::parse`/`fromCents` em parsers, mapper, exporter e API; API rejeita `55.90` float com 422 (`ApiTransactionsTest.php:67`).
- **Lógica de negócio em Resource/Controller/Livewire: não.** Pages `Import`, `Rules`, `Backup` só orquestram o Domain; `TransactionController::filtered` só monta `where`s de listagem (sem matemática de dinheiro nem transição de estado — mutações vão a `Create/Update/DeleteTransaction`, pagamento a `PayInvoice`).
- **`TODO` sem entrada no ROADMAP: nenhum.** Grep por `\bTODO\b|\bFIXME\b|\bXXX\b|\bHACK\b` em `app/` retorna vazio (o match anterior de `TODO` era substring de "TODOS" em comentário).
- **Suposições registradas:** `[8.1]`, `[8.2a]`×3, `[8.2b]`, `[8.3]`×2, `[8.4a]`×7, `[8.4b]`×2, `[8.5]`×5, `[8.6]`×5 em `docs/QUESTIONS.md:107-132`, todas com comportamento correspondente nos testes.

## Gaps e tasks corretivas sugeridas (formato `- [ ] N.Fx descrição (aceite: ...)`)

Nenhum gap: zero FALHAs, zero tasks `[!]`/`[-]` na fase, `CHECK: PASS` com 962 testes. Nenhuma task corretiva `8.Fx` a criar. Observações opcionais (não-bloqueantes, sem corretiva): (a) staging→confirm fim a fim exercido com Nubank CSV e OFX — Itaú/Inter CSV cobertos no nível parser/preset/mapeamento, o pipeline é genérico sobre o mapping; (b) `Rule` tem `LogsActivity` mas nenhum teste afirma linhas de `activity_log` para regras (SPEC 2.18) — um teste espelhando `TransactionSchemaTest.php:249` fecharia a prova.

Veredito: APROVADA
