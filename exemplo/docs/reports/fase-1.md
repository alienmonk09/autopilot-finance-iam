Review #1


# Review da fase 1 — Bancos, Contas, Categorias, Tags, Payees

## Resultado do check
Comando `scripts/check.sh` (único aceite, AGENTS.md regra 8). Saída literal:
```
{"tool":"pint","result":"passed"}{"tool":"phpstan","result":"passed","errors":0}
{"tool":"pest","result":"passed","tests":180,"passed":180,"assertions":653,"duration_ms":2210}
CHECK: PASS
```
Tasks `[!]`/`[-]` na fase 1: nenhuma (a única `[-]` do roadmap é a 9.3, fase 9). Confirmado: ROADMAP fase 1 só tem `[x]` 1.1–1.5.

## Aceite da fase
Aceite da fase (ROADMAP.md:18): "CRUD completo; saldo calculado correto em testes com todos os tipos de transação simulados por factory".

- CRUD completo (banks, accounts, categories, tags, payees) → OK. Migrations `database/migrations/2026_09_03_050001_create_banks_table.php`, `..._050003_create_accounts_table.php`, `..._050002_create_categories_table.php`, `..._050004_create_tags_table.php`, `..._050005_create_payees_table.php`, `..._050006_create_taggables_table.php`; models `app/Models/Bank.php`, `app/Models/Account.php`, `app/Models/Category.php`, `app/Models/Tag.php`, `app/Models/Payee.php`; factories `database/factories/BankFactory.php`, `AccountFactory.php`, `CategoryFactory.php`, `TagFactory.php`, `PayeeFactory.php`; Resources `app/Filament/App/Resources/BankResource.php`, `AccountResource.php`, `CategoryResource.php`, `TagResource.php`, `PayeeResource.php` registrados no painel (`tests/Feature/Filament/CatalogResourcesTest.php:32-38`). Provas: factories válidas `tests/Feature/Catalogs/CatalogModelsTest.php:20-37`; defaults de conta `CatalogModelsTest.php:39-53`; isolamento por usuário + policies `CatalogModelsTest.php:87-133`; CRUD via painel (conta com "1.234,56"→123456 em `CatalogResourcesTest.php:130-152`, categoria/sub `CatalogResourcesTest.php:154-176`, banco com slug `CatalogResourcesTest.php:178-192`, etiqueta unique `CatalogResourcesTest.php:194-211`, merge via UI `CatalogResourcesTest.php:213-226`); listas exigem login e respondem OK `CatalogResourcesTest.php:100-114`; sidebar PT sem placeholders `CatalogResourcesTest.php:116-128`.
- 1.1 Migrations/models/factories → OK. Evidência acima + `CatalogModelsTest.php:55-85` (visibleFor banks/categories), `CatalogModelsTest.php:135-148` (slug unique, nullOnDelete), `CatalogModelsTest.php:150-153` (taggables polimórfica).
- 1.2 Seeders: bancos BR com cores, categorias clonadas no registro via evento → OK. `app/Domain/Banks/BankCatalog.php:19-62` (42 entradas, Nubank `#820AD1`, Itaú `341`, Caixa `104`, + `carteira-dinheiro`/`outro`); `database/seeders/BankSeeder.php:20-31` (updateOrCreate por slug, DB::transaction); `app/Domain/Categories/DefaultCategoryCatalog.php` + `database/seeders/SystemCategorySeeder.php:20-53`; `app/Domain/Categories/CloneDefaultCategories.php:18-74` chamada em `app/Observers/UserObserver.php:21-25` (created). Testes: `tests/Feature/Catalogs/SeedersTest.php:16-29` (≥42, cores/codes, genéricos), `:31-39` (idempotente), `:51-77` (topos/subs/especiais is_system + máx 2 níveis), `:79-86` (idempotente), `:88-110` (clona no `User::factory()->create` preservando hierarquia + is_system), `:112-121` (não duplica), `:123-131` (não apaga própria preexistente).
- 1.3 `AccountBalanceService` (saldo em data, saldo previsto) → OK. Código `app/Domain/Accounts/AccountBalanceService.php:38-72` (current/at/forecast, cutoff inclusivo, TIMEZONE `America/Sao_Paulo:30`), `BalanceEntry.php:1-83` (DTO puro, date Y-m-d sem hora, amount positivo), `BalanceEntryType.php`/`BalanceEntryStatus.php`. Todos os 6 tipos de SPEC 2.8 cobertos: `tests/Unit/Domain/Accounts/AccountBalanceServiceTest.php:55-68` (income/expense cleared+reconciled, pending fora), `:70-80` (futuros/pendentes fora), `:82-94` (asOf explícito), `:96-116` (transfer origem−/destino+, nulo entre outras, self-transfer zero), `:118-131` (credit_card_payment −, credit_card_expense ignorado com/sem conta, adjustment +), `:133-145` (corte inclusivo), `:147-161` (initial_balance_date: 0 antes, conta sem data), `:163-182` (forecast inclui pending até D + `$projected` recorrência/fatura, corta após D), `:184-198` (datas inválidas → AccountException), `:200-208` (retorna Money `R$ 1.000,00` sem float). Nota "por factory": tabela `transactions` só existe na 2.1; a simulação é via DTO `BalanceEntry` (documentado em `BalanceEntry.php:14-18` e `docs/QUESTIONS.md [1.3]`), com factories reais para os 5 cadastros; sem factory de transação exigível nesta fase (ROADMAP 1.1 não a lista).
- 1.4 Filament Resources: Accounts (cards + tabela), Categories (árvore/drag order), Tags, Payees (merge), Banks (custom) → OK. Accounts: `AccountResource.php:163-196` (tabela banco/conta/tipo/saldo/cor/flags, filtro ocultar-arquivadas) + `Pages/ListAccounts.php:19-24` (header widget) + `Widgets/AccountStats.php:25-40` (cards Patrimônio/Contas); coluna "Saldo atual" via service (`AccountResource.php:178-180`, lista vazia até fase 2 — cf. `docs/QUESTIONS.md [1.4]`). Categories: `CategoryResource.php:125-157` (colunas nome/pai/tipo/origem, filtro tipo + ocultar-arquivadas, sort `sort_order`) + ações `moveUp`/`moveDown` (`CategoryResource.php:180-195` → `ReorderCategory`), hierarquia 2 níveis validada (`CategoryDepthTest.php:11-63`, `ReorderCategoryTest.php:13-72`, `ArchiveAndDeletionGuardTest.php:93-110`). Tags: `TagResource.php:47-82` simples + unique por usuário. Payees: `PayeeResource.php:86-127` + merge via `MergePayees` (`PayeeResource.php:99-124`); dedupe normalizado (`PayeeDedupeTest.php:12-63`, `PayeeNormalizerTest.php:7-18`); merge preserva/adota (`MergePayeesTest.php:14-52`, rejeições `:54-74`). Banks: `BankResource.php:50-111` (visíveis globais+próprios, form sem slug — gerado auto, cf. `docs/QUESTIONS.md [1.4]`); globais/sistema readonly (`CatalogResourcesTest.php:78-98`, 403 em `:228-242`).
- 1.5 Arquivamento/bloqueio de exclusão (regra 3.13) → OK. `app/Domain/Accounts/ArchiveAccount.php:16-30`, `app/Domain/Categories/ArchiveCategory.php`, `app/Domain/Accounts/AccountDeletionGuard.php:34-55` (forward-compatible via `Schema::hasTable/hasColumn`, cf. `docs/QUESTIONS.md [1.5]`), `app/Domain/Categories/CategoryDeletionGuard.php` + `ReallocateCategory.php`, observers `app/Observers/AccountObserver.php:19-22` (+ Category/Payee observers). Testes: conta arquiva/des-arquiva `:ArchiveAndDeletionGuardTest.php:21-34`, exclui sem vínculo `:36-43`, bloqueia com vínculo (tabela `transactions` fake) `:45-69`; categoria arquiva `:71-82`, exclui sem uso `:84-91`, exige realocação (subs `:93-110`, payees `:112-128`), valida destino `:130-155`, sistema imune `:157-164`, scopes active/archived `:166-191`; UI arquiva/des-arquiva/exclui-bloqueada/filtros (`ArchiveActionsTest.php:23-110`).

## Regras da seção 3 tocadas
Fase 1 toca 3.1 e 3.13 (transversais 3.14/3.15 reutilizadas). 3.2–3.12 têm implementação e testes previstos nas fases 2–6 e não são exigíveis aqui (não são FALHA).

- 3.1 Saldo de conta = inicial + Σ(income) − Σ(expense) − Σ(transfer origem) + Σ(transfer destino) − Σ(credit_card_payment) ± adjustment, só cleared/reconciled com date ≤ hoje; previsto inclui pending ≤ D + recorrências não materializadas + faturas em aberto → OK. Código `AccountBalanceService.php:85-113` (fórmula), `:120-147` (efeito por tipo, transfer casa dois lados), `:154-171` (initial_date). Testes `AccountBalanceServiceTest.php:55-182` (todos os tipos, status, datas, previsto com `$projected` generator `:174-178` provando 330554 vs atual 665000 em `:180-181`).
- 3.13 Exclusão de conta/cartão com transações → bloqueada + arquivar; categoria com transações → realocar → OK (parcial cartão diferido). Conta/categoria provadas acima (`ArchiveAndDeletionGuardTest.php:45-69`, `:93-128`, `ArchiveActionsTest.php:59-82`). Cartão (`credit_cards`) não existe antes da fase 3 — sem código nem teste exigível aqui; `AccountDeletionGuard.php:24-32` já lista `credit_cards.payment_account_id` como referência futura.
- 3.14 Datas e timezone (America/Sao_Paulo, date DATE puro) → OK. `AccountBalanceService.php:30` (TIMEZONE), `BalanceEntry.php:59-82` (só Y-m-d válido, sem hora/conversão). Testes `AccountBalanceServiceTest.php:141-145` (string/Carbon/DateTimeImmutable), `:184-198` (rejeita 'agosto', '2026-13-01', '2026-02-30', '10/08/2026').
- 3.15 Dinheiro nunca float, centavos, parse "1.234,56"/"1234.56", exibe R$ → OK (herdado fase 0, exercido aqui). `AccountBalanceService.php:92` (int), `Money.php`; teste `AccountBalanceServiceTest.php:200-208` (Money, cents 100000, `R$ 1.000,00`); form `AccountResource.php:110-138` (Money::parse/dehydrate + MoneyInputRule) provado por `CatalogResourcesTest.php:130-152`.
- 3.2–3.12 → não tocadas pela fase 1 (sem código nem teste exigido aqui; cobertura virá nas fases 2–6). Sem FALHA.

## Sinais de contorno encontrados
- teste `skip` / `markTestSkipped`: nenhum (grep em `tests/*.php` só achou "comportamento" em comentário de `tests/Support/OwnedWidgetPolicy.php:10` e "factories" no nome de teste).
- asserção trivial: nenhuma — 653 assertions em 180 testes, todas com valores concretos (ex.: `AccountBalanceServiceTest.php:67` 658520, `:180` 330554, `SeedersTest.php:21` ≥42, `CatalogResourcesTest.php:149` 123456).
- código de produção citando mock/teste: nenhum em `app/`.
- float em dinheiro: uso legítimo e isolado — `Money.php:55` `toFloat()` + `Money.php:93-95` `multiply(int|float)` com round; armazenamento é int centavos, `AccountBalanceService.php:92` soma int, teste `AccountBalanceServiceTest.php:200-208` prova sem float. Não é contorno.
- lógica de negócio em Resource/Controller/Livewire: nenhuma — Resources só declaram form/tabela e delegam a Domain (`ArchiveAccount` em `AccountResource.php:208-209,217-218`, `AccountDeletionGuard` em `:224-234`, `ReorderCategory` em `CategoryResource.php:185-194`, `MergePayees` em `PayeeResource.php:120-124`, `AccountBalanceService` só leitura em `AccountResource.php:180`). `AccountStats.php:25-28` soma `initial_balance_cents` — agregação de exibição documentada como placeholder até fase 2 (`docs/QUESTIONS.md [1.4]`), não regra de negócio.
- `TODO`/`FIXME` sem entrada no ROADMAP: nenhum em `app/` ou `tests/` (grep só achou "Nunca float" em comentário de `Money.php:13`).

## Gaps e tasks corretivas sugeridas
Nenhuma FALHA, logo nenhuma corretiva `1.Fx`. Observações diferidas por design (registradas em `docs/QUESTIONS.md`, viram implementação nas fases indicadas, não são FALHA): (a) `AccountResource` coluna/widget de saldo usa lista vazia de lançamentos até a 2.2 alimentar `BalanceEntry` reais ([1.4]); (b) `MergePayees`/`ReallocateCategory` movem só vínculos existentes (payees/categorias) — mover `transactions.*` entra na fase 2 ([1.4]/[1.5]); (c) guarda de exclusão checa só tabelas existentes via `Schema::hasTable` ([1.5]); (d) auditoria SPEC 2.18 (`activity_log`) entra na 2.1; household SPEC 2.17 proibido antes da fase 9.

Veredito: APROVADA
