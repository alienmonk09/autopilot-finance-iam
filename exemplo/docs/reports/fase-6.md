Review #1


## Resultado do check
```
{"tool":"pint","result":"passed"}{"tool":"phpstan","result":"passed","errors":0}
{"tool":"pest","result":"passed","tests":722,"passed":722,"assertions":2922,"duration_ms":13410}
CHECK: PASS
```
`scripts/check.sh` verde: pint passou, phpstan passou com 0 erros, pest 722/722 passou (2922 asserções).

## Aceite da fase (item → OK/FALHA + evidência)
- spent correto por data → OK. Implementação `app/Domain/Budgets/BudgetCalculator.php:38` (`spent()`, filtra `whereDate` no mês, tipos `expense`+`credit_card_expense`). Teste `tests/Feature/Budgets/BudgetCalculatorTest.php:50` (soma 5590+23456+8990 por data, exclui mês vizinho) e `:156` (limites 30/09 conta, 01/10 não).
- spent por competência (respeita `budget_basis`) → OK. Implementação `app/Domain/Budgets/BudgetCalculator.php:92` (`dateColumnFor()`: `date` vs `competence_date` via `user_settings.budget_basis`). Teste `tests/Feature/Budgets/BudgetCalculatorTest.php:66` (mesmo lançamento cai em 2026-09 por data e em 2026-08 por competência).
- spent com subcategorias (e splits, global, ignored) → OK. Implementação `app/Domain/Budgets/BudgetCalculator.php:103` (`scopeCategoryIds()`: categoria + filhas diretas; `null` = global) e `:59-71` (splits no escopo, excluindo `is_ignored_in_reports` e soft-deleted). Testes `tests/Feature/Budgets/BudgetCalculatorTest.php:82` (subcategoria soma, fora do escopo não), `:89` (ignored/soft-deleted/income/transfer/payment/adjustment/outro usuário não somam), `:125` (splits contam só no escopo), `:138` (global soma tudo).
- rollover (carrega `budgeted−spent` quando ativo) → OK. Implementação `app/Domain/Budgets/BudgetRolloverService.php:56` (`runFor()` em `DB::transaction`, `updateOrCreate` por budget+mês, `carried = budgeted+carried−spent` anterior, 0 sem anterior ou sem rollover). Testes `tests/Feature/Budgets/BudgetRolloverTest.php:46` (primeiro mês carried 0), `:58` (sobra 70000 acumula para 120000 em 2 meses), `:78` (estouro carrega negativo), `:87` (sem rollover zera), `:100` e `:113` (idempotente, só ativos).
- alertas disparam uma vez por faixa → OK. Implementação `app/Domain/Budgets/CheckBudgetAlerts.php:56` (`check()`: faixas inclusivas `>= threshold` e `>= 100`, dedupe por type+data JSON budget+mês+faixa em `alreadyNotified()`, respeita `notify_budget_threshold`) delegada por `app/Jobs/BudgetAlertJob.php:32` (`handle()` só delega; fila database). Testes `tests/Feature/Budgets/BudgetAlertJobTest.php:53` (abaixo nada), `:61` (85% → 1× `threshold` com payload budget/mês/spent/budgeted), `:81` (120% → 2× `threshold`+`overrun`), `:90` (dupla execução não duplica), `:99` (crescimento entre execuções dispara só a faixa nova), `:118` (opt-out silencia), `:127`+`:141` (command `budgets:alert` idempotente e agendado `0 0 * * *`).
- metas calculam aporte/projeção → OK. Implementação `app/Domain/Goals/GoalProjectionService.php:24` (`contributedCents`/`savedCents`=`initial`+aportes/`remainingCents`/`progressPct`=`intdiv`/`monthlyNeededCents`/`projectedDate`/`summary()` em centavos int, sem float). Testes `tests/Feature/Goals/GoalProjectionServiceTest.php:37` (170000/14%/restante), `:78` (mensal inclui mês atual, 275000), `:85` (ceil 333334), `:92` (null sem prazo), `:98` (prazo vencido = restante), `:104` (ritmo médio → 2027-09-03), `:120` (null sem aportes), `:126` (summary completo).
- aporte rápido com transferência opcional → OK. Implementação `app/Domain/Goals/AddGoalContribution.php:36` (`add()` em `DB::transaction`, cria `GoalContribution` + `transfer` origem→conta da meta via `TransferService`, `cleared`, "Aporte {meta}"; rejeita float, valor ≤0, sem conta vinculada, origem=destino, origem de outro usuário, meta não-ativa). Testes `tests/Feature/Goals/AddGoalContributionTest.php:43` (manual sem transação), `:60` ("1.234,56"→123456), `:71` (transfer com tipos/status/contas corretos), `:92`+`:106`+`:120` (atomicidade: falha não cria nem aporte nem transação), `:135` (0/negativo/float rejeitados), `:148`+`:164` (arquivada/concluída/outro usuário rejeitam).
- migrations/models/factories (6.1) → OK. Migrations `database/migrations/2026_09_03_090001_create_budgets_table.php`, `...090002_create_budget_periods_table.php`, `...090003_create_goals_table.php`, `...090004_create_goal_contributions_table.php`. Testes `tests/Feature/Budgets/BudgetSchemaTest.php:81` (colunas SPEC 2.12 + índices), `:101` (FK user CASCADE, category SET NULL), `:108` (budget_periods + unique budget+mês), `:130`+`:155` (factories realistas), `:199`+`:234`+`:246` (isolamento scope/policy/cascade/guard 3.13); `tests/Feature/Goals/GoalSchemaTest.php:70`+`:90`+`:97`+`:113` (colunas 2.13 + FKs), `:120`+`:141`+`:276` (factories, aporte manual válido), `:168`+`:184`+`:202`+`:214` (scope/policy/cascade/guard 3.13).
- UI orçamento mensal com barras/copiar mês anterior/sugerir pela média (6.3) → OK. Página `app/Filament/App/Pages/Budgets.php:158` (`rows()`: orçado/gasto/restante/barra via `BudgetCalculator`, rollover via `BudgetPeriod`), `:209` (`evolution()` 12 meses), actions `copiarMesAnterior` (`app/Domain/Budgets/CopyBudgetsFromPreviousMonth.php:33`, `DB::transaction`, idempotente) e `sugerirMedia` (`app/Domain/Budgets/SuggestBudgetAverages.php:33`, média 3 meses cheios, só exibe; aplica com confirmação). Testes `tests/Feature/Budgets/CopyBudgetsFromPreviousMonthTest.php:40`+`:59`+`:70`+`:98`+`:132` (copia valores/flags, idempotente, pula existentes, mais recente, ignora inativos/anuais/encerrados, global, outro usuário); `tests/Feature/Budgets/SuggestBudgetAveragesTest.php:44`+`:53`+`:74`+`:97` (média, sem histórico=0, intdiv, escopo); `tests/Feature/Filament/BudgetsPageTest.php:65`+`:80`+`:90` (barras 85% Atenção / 120% Estourado / rollover), `:106` (navegação mensal), `:134`+`:156` (copiar idempotente, sugerir sem salvar + aplicar), `:177`+`:201`+`:223`+`:240` (CRUD centavos, arquivar/reativar, isolamento), `:261` (evolução 12 meses).
- UI metas com aporte rápido gerando transferência opcional (6.4) → OK. Página `app/Filament/App/Pages/Goals.php:82` (`cards()` via `summary()`), `:117` (`historico()`), action `aporteRapido` (`:466`, delega a `AddGoalContribution`, exibe erro de domínio sem persistir parcial). Testes `tests/Feature/Filament/GoalsPageTest.php:56` (card 12%/valores/prazo), `:78` (manual sem transação), `:97` (com transferência origem→cofre vinculada), `:116` (histórico), `:128`+`:146`+`:163` (criar/editar/concluir/arquivar/reabrir), `:181` (outro usuário não vê nem aporta).

## Regras da seção 3 tocadas (regra → OK/FALHA + teste)
- 3.12 spent respeita `budget_basis` → OK. Teste `tests/Feature/Budgets/BudgetCalculatorTest.php:66`.
- 3.12 spent inclui subcategorias (e splits) → OK. Testes `tests/Feature/Budgets/BudgetCalculatorTest.php:82`, `:125`, `:138`.
- 3.12 rollover carrega `budgeted−spent` (positivo ou negativo) quando ativo; 0 caso contrário/sem anterior → OK. Testes `tests/Feature/Budgets/BudgetRolloverTest.php:58`, `:78`, `:87`.
- 3.12 alerta dispara uma única vez por período ao cruzar `alert_threshold_pct` e outra ao cruzar 100% → OK. Testes `tests/Feature/Budgets/BudgetAlertJobTest.php:61`, `:81`, `:90`, `:99`.
- 3.10 (tocada via splits no spent) Σ splits == amount nos relatórios → OK. Teste `tests/Feature/Budgets/BudgetCalculatorTest.php:125` (relatórios usam splits).
- 3.13 (tocada via guards) excluir categoria em uso bloqueada / conta vinculada à meta bloqueada → OK. Testes `tests/Feature/Budgets/BudgetSchemaTest.php:246`, `tests/Feature/Goals/GoalSchemaTest.php:214`.
- 3.15 (tocada via Money) nunca float, parse "1.234,56" → OK. Testes `tests/Feature/Goals/AddGoalContributionTest.php:60`, `tests/Feature/Filament/BudgetsPageTest.php:177` (`valor '500,00'`→50000), `tests/Feature/Filament/GoalsPageTest.php:128` (`alvo '15.000,00'`→1500000).

## Sinais de contorno encontrados
- Nenhum `skip`/`markTestSkipped`/asserção trivial nos testes da fase (`tests/Feature/Budgets/*`, `tests/Feature/Goals/*`, `tests/Feature/Filament/{BudgetsPage,GoalsPage}Test.php`): buscas por `skip\(|markTestSkipped|assertTrue\(true\)|TODO` retornaram zero.
- Nenhum `TODO`/placeholder/"em construção" em `app/Domain/Budgets/*`, `app/Domain/Goals/*`, `app/Filament/App/Pages/Budgets.php`, `app/Filament/App/Pages/Goals.php` (ocorrências de "placeholder" são só labels de form Filament, ex. `Budgets.php:447`; ocorrências de "float" são comentários "nunca float" e o guard `AddGoalContribution.php:113` que rejeita float).
- Nenhum código de produção citando mock/teste; dinheiro sempre em centavos int (`intdiv`, casts `(int)`, `Money::parse()->cents()`; float lançado como exceção).
- Lógica de negócio em `app/Domain/Budgets/*` e `app/Domain/Goals/*`; Pages só orquestram (`Budgets.php:169,651,676`, `Goals.php:84,532`) — o cálculo de `%` nas linhas da Page (`Budgets.php:184`, `:250`) é apresentação com a mesma convenção documentada em `docs/QUESTIONS.md [6.3]` e coberta por `BudgetsPageTest.php:65,80`.
- `DB::transaction` em toda operação multi-tabela: `BudgetRolloverService.php:57`, `CopyBudgetsFromPreviousMonth.php:42`, `AddGoalContribution.php:84` (com teste de atomicidade em `AddGoalContributionTest.php:92,106,120`).
- Jobs idempotentes com teste de dupla execução: `BudgetAlertJobTest.php:90` (2×dispatch→2 notificações), `BudgetRolloverTest.php:100,113`, `CopyBudgetsFromPreviousMonthTest.php:59`.
- Fixtures realistas em PT em todos os testes (iFood, Supermercado, Restaurante, Padaria, Uber, Salário, NuConta Nubank, Conta Poupança Inter, Reserva de emergência, Viagem Japão) com valores plausíveis em centavos.

## Gaps e tasks corretivas sugeridas (formato `- [ ] N.Fx descrição (aceite: ...)`)
- Nenhum. Tasks `[!]`/`[-]` na fase: nenhuma. Todo o aceite e a regra 3.12 estão cumpridos com testes; `scripts/check.sh` verde com 722 testes.

Veredito: APROVADA
